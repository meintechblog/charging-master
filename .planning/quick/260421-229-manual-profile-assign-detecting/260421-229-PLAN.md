---
id: 260421-229
slug: manual-profile-assign-detecting
description: Manual profile assignment while a session is still in `detecting`
created: 2026-04-21
mode: quick
---

# Quick Task 260421-229 — Manual profile assignment during `detecting`

## Goal

Let the user manually assign a device profile to an active session while the
session is in state `detecting` (i.e. before DTW matching has produced — or
failed to produce — a match). Today this is only exposed after the detection
buffer is exhausted (via `UnknownDeviceDialog`, ~10 min wait) or after a match
has already landed (via the existing "Profil ändern" link in the active banner).

## Why

- E-bike charger mismatch made the current PowerTube session impossible to
  match (ref curve startPower 106 W vs live 172 W). User wants to override
  without waiting 10 min for the dialog.
- The backend already supports this: `PUT /api/charging/sessions/:id` with
  `{profileId, targetSoc}` triggers `ChargeMonitor.overrideSession()`, which
  synthesizes a match and pushes the state to `matched`. No backend work
  required.

## Scope

**One file:** `src/components/charging/charge-banner.tsx`

In the `detecting` render branch (currently only shows the spinner), add a
compact inline "Profil manuell zuweisen" link that reuses the existing
`profiles` state + `handleOverrideProfile` callback (both already defined in
the component for the active-state branch). Click → inline profile chip row →
`PUT /api/charging/sessions/:id` → ChargeMonitor flips session to `matched`.

No schema change. No new API. No changes to `UnknownDeviceDialog`.

## Tasks

### T1 — Add manual-assign control to `detecting` branch

- **File:** `src/components/charging/charge-banner.tsx`
- **Action:** In the `if (session.state === 'detecting')` block, below the
  existing "Gerät wird erkannt..." spinner row, render a second row with a
  small "Profil manuell zuweisen" button that toggles the existing
  `showProfileDropdown` state, rendering the same profile chip list used in
  the active-state branch (lines ~426-447). Wire chips to the existing
  `handleOverrideProfile(profileId)`.
- **Verify:** During a `detecting` session on `/devices/[id]`, the control is
  visible, clicking it reveals profile chips, clicking a chip fires the PUT
  and the banner transitions to `matched` within one SSE tick.
- **Done:** Component compiles, SSE-driven state transition works end-to-end.

### T2 — Deploy to LXC + sync version

- **Action:** `scp` updated file to `/opt/charging-master/src/components/charging/charge-banner.tsx`,
  regenerate `src/lib/version.ts` via `scripts/build/generate-version.mjs`
  after commit, `scp` the new `version.ts` too, restart the service.
- **Verify:** `/api/update/status` returns `updateAvailable: false` and
  `currentSha === remote.sha` matches the new commit.
- **Done:** Live LXC shows the new banner UI and no update banner.

## must_haves

- manual-profile-dropdown-in-detecting-branch
- no-schema-change
- existing-unknown-device-dialog-untouched
- lxc-on-same-sha-as-origin
