---
id: 260421-34c
slug: auto-scan-on-devices-page
description: Auto-run discovery scan + clickable IPs + registered-row layout polish
date: 2026-04-21
status: complete
commits:
  - f4b4f49  # feat(devices): auto-scan on /devices mount + clickable IP links
  - 90b16c5  # chore(devices): move watts next to toggle, drop redundant relay-state text
---

# Quick Task 260421-34c — Summary

## What changed

- `src/components/devices/discovery-list.tsx`
  - Converted `handleScan` to `useCallback`.
  - Added `didAutoScanRef` + `useEffect` that fires `handleScan()` once
    on mount — navigating to `/devices` now starts the subnet scan
    without a click. Re-scan via the button still works; StrictMode
    double-mount in dev can't double-fire thanks to the ref guard.
  - Each row's IP is now an `<a target="_blank" rel="noopener
    noreferrer" href="http://<ip>/">` → opens the Shelly admin UI in
    a new tab.

- `src/app/devices/device-manager.tsx`
  - Same clickable-IP pattern in `RegisteredDeviceRow`.
  - Dropped the `<span>` that printed "Relais: Ein / Aus" — the toggle
    already visualizes that state; keeping both was noise.
  - Moved the live-watts readout out of the left info line and into
    the right controls block, right before the toggle (`w-14` with
    right-aligned tabular nums so the number doesn't jitter as power
    fluctuates). Charge-active badge stays in the info line.

## Verification

- LXC + origin on `4526f70`, `updateAvailable: false`.
- No server-side curl smoke test per the 260421-146 rule —
  verification done in the browser.

## Related follow-up (separate task)

- Backlog Phase 999.1 added during this task: Multi-Channel Shelly
  Support — drop all `?id=0` hardcoding + schema migration +
  surface `Switch.GetConfig.name`. See
  `.planning/phases/999.1-multi-channel-shelly-support/` + the
  Backlog section in ROADMAP.md.
