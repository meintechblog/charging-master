---
id: 260421-6f6
slug: umlaut-fixes
description: Replace ASCII digraphs (ue/ae/oe) with proper umlauts in user-facing strings
date: 2026-04-21
status: complete
commits:
  - f72ef68  # chore(i18n): replace ASCII ue/ae/oe digraphs with proper umlauts
---

# Quick Task 260421-6f6 — Summary

## What changed

Nine occurrences of ASCII digraph umlauts (`ue`, `ae`, `oe`) in
user-facing strings were replaced with proper `ü / ä / ö` across
five files. The app serves utf-8 throughout, so there was no
encoding reason to keep the digraph form.

| File | What |
|------|------|
| src/modules/notifications/notification-service.ts | "fuer" → "für" |
| src/components/devices/add-device-form.tsx | "Geraete-ID", "Hinzufuegen", "Geraet", "hinzugefuegt" |
| src/app/history/page.tsx | "Alle Geraete" |
| src/app/profiles/[id]/page.tsx | "Ladevorgaenge fuer" |
| src/app/history/[sessionId]/page.tsx | "Zurueck zur Uebersicht", "Messdaten fuer" |

No behavioral change, no schema change.

## Verification

- Grep sweep for `Geraete|laengere|ueber|fuer|Fuer|muessen|hinzufuegen|...`
  returns no matches after the edit.
- LXC + origin on `f72ef68`.
- Browser: user will see real umlauts in the affected surfaces.
