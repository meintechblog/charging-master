---
phase: 06-device-discovery-mqtt-removal
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - server.ts
  - src/types/global.d.ts
  - src/app/settings/page.tsx
  - src/components/layout/sidebar.tsx
  - src/app/api/devices/route.ts
  - src/db/schema.ts
  - package.json
autonomous: false
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03
  - CLEAN-04

must_haves:
  truths:
    - "The app starts and runs without any MQTT broker"
    - "mqtt.js is not in package.json"
    - "src/modules/mqtt/ directory does not exist"
    - "Settings page shows only Pushover settings, no MQTT section"
    - "Sidebar shows no MQTT status indicator"
    - "server.ts has no MQTT imports, service instantiation, or global assignment"
  artifacts:
    - path: "server.ts"
      provides: "Clean server startup without MQTT"
      contains: "HttpPollingService"
    - path: "src/types/global.d.ts"
      provides: "Global types without MQTT references"
    - path: "src/app/settings/page.tsx"
      provides: "Settings page with Pushover only"
    - path: "src/components/layout/sidebar.tsx"
      provides: "Sidebar without MQTT status"
  key_links:
    - from: "server.ts"
      to: "src/modules/shelly/http-polling-service.ts"
      via: "import and instantiation"
      pattern: "HttpPollingService"
    - from: "package.json"
      to: "no mqtt dependency"
      via: "dependencies section"
      pattern: "mqtt.*MUST NOT EXIST"
---

<objective>
Completely remove all MQTT code, dependencies, UI, and references from the codebase.

Purpose: After Phase 5 moved data flow to HTTP and Plan 01 replaced MQTT discovery with HTTP scanning, MQTT is fully dead code. This plan surgically removes every trace: the mqtt.js package, MqttService module, MQTT settings UI, MQTT API routes, MQTT globals, MQTT status indicator, and MQTT config rows from the database. The app will start and run with zero MQTT dependency.

Output: Clean codebase with no MQTT references. App boots with only HttpPollingService. Settings page shows only Pushover. Sidebar shows no MQTT status.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-device-discovery-mqtt-removal/06-CONTEXT.md
@.planning/phases/06-device-discovery-mqtt-removal/06-01-SUMMARY.md

@server.ts
@src/types/global.d.ts
@src/app/settings/page.tsx
@src/components/layout/sidebar.tsx
@src/components/settings/mqtt-settings.tsx
@src/db/schema.ts
@src/app/api/devices/route.ts
@src/app/api/mqtt/test/route.ts
@src/app/api/mqtt/status/route.ts
@package.json

<interfaces>
<!-- After Plan 01, these are the only globals needed -->
From src/types/global.d.ts (target state):
  var __httpPollingService: HttpPollingService;
  var __eventBus: EventBus;
  var __chargeMonitor: ChargeMonitor;

<!-- server.ts target: no MqttService, no __mqttService, no __discoveredDevices -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove MQTT module, API routes, settings UI, sidebar status, and package dependency</name>
  <files>server.ts, src/types/global.d.ts, src/app/settings/page.tsx, src/components/layout/sidebar.tsx, src/components/settings/mqtt-settings.tsx, src/app/api/mqtt/test/route.ts, src/app/api/mqtt/status/route.ts, src/modules/mqtt/mqtt-service.ts, src/modules/mqtt/discovery.ts, src/modules/mqtt/shelly-parser.ts, src/db/schema.ts, src/app/api/devices/route.ts, package.json</files>
  <read_first>
    - server.ts (identify all MQTT lines to remove)
    - src/types/global.d.ts (identify MQTT globals to remove)
    - src/app/settings/page.tsx (identify MQTT settings section to remove)
    - src/components/layout/sidebar.tsx (identify MQTT status indicator to remove)
    - src/db/schema.ts (identify mqttTopicPrefix column)
    - src/app/api/devices/route.ts (identify mqttTopicPrefix usage)
    - package.json (identify mqtt dependency)
  </read_first>
  <action>
**Delete files (per D-10, D-12):**
- Delete `src/modules/mqtt/mqtt-service.ts`
- Delete `src/modules/mqtt/discovery.ts`
- Delete `src/modules/mqtt/shelly-parser.ts`
- Delete `src/modules/mqtt/` directory entirely (per D-10)
- Delete `src/app/api/mqtt/test/route.ts` (per D-12)
- Delete `src/app/api/mqtt/status/route.ts` (per D-12)
- Delete `src/app/api/mqtt/` directory entirely
- Delete `src/components/settings/mqtt-settings.tsx` (per D-09)

**Clean server.ts (per D-13):**
Remove these specific elements:
- Line 3: `import { MqttService } from './src/modules/mqtt/mqtt-service';` -- DELETE
- Line 9: `import type { DiscoveredDevice } from './src/modules/mqtt/discovery';` -- DELETE
- Line 31: `const mqttService = new MqttService(eventBus);` -- DELETE
- Lines 34-46: The entire MQTT settings loading block (mqttHost, mqttPort, mqttUsername, mqttPassword, the if/else connecting to MQTT) -- DELETE
- Line 62: `globalThis.__mqttService = mqttService;` -- DELETE
- Lines 70-83: The entire `globalThis.__discoveredDevices = new Map(...)` block and the `eventBus.on('online:*', ...)` handler that populates it -- DELETE
- In shutdown function: `await mqttService.disconnect();` -- DELETE

The remaining server.ts should have: createServer, next, HttpPollingService, EventBus, ChargeMonitor, NotificationService, SessionRecorder imports. Globals: __eventBus, __httpPollingService, __chargeMonitor only.

**Clean global.d.ts (per D-14):**
Remove:
- `import type { MqttService } from '@/modules/mqtt/mqtt-service';`
- `import type { DiscoveredDevice } from '@/modules/mqtt/discovery';`
- `var __mqttService: MqttService;`
- `var __discoveredDevices: Map<string, DiscoveredDevice>;`

Keep:
- `import type { HttpPollingService } from '@/modules/shelly/http-polling-service';`
- `import type { EventBus } from '@/modules/events/event-bus';`
- `import type { ChargeMonitor } from '@/modules/charging/charge-monitor';`
- `var __httpPollingService: HttpPollingService;`
- `var __eventBus: EventBus;`
- `var __chargeMonitor: ChargeMonitor;`

**Clean settings page (per D-09):**
In `src/app/settings/page.tsx`:
- Remove `import { MqttSettings } from '@/components/settings/mqtt-settings';`
- Remove the entire `<SettingsSection title="MQTT Broker" ...>` block
- Keep the Pushover settings section

**Clean sidebar (per D-04 implied -- no MQTT status):**
In `src/components/layout/sidebar.tsx`:
- Delete the entire `useMqttStatus()` function (lines 15-45)
- Remove `const mqttConnected = useMqttStatus();` from Sidebar component
- Remove the MQTT status indicator div at the bottom of the sidebar (the div with "MQTT Verbunden/Getrennt")
- Remove the `useState, useEffect` imports if only used by useMqttStatus (check if useActiveLearnCount also uses them -- it does, so keep them)

**Clean schema (mqttTopicPrefix column):**
In `src/db/schema.ts`: Remove line `mqttTopicPrefix: text('mqtt_topic_prefix').notNull(),` from the plugs table. NOTE: Since this is SQLite and the column exists in the DB, the app will simply ignore it. No migration needed -- Drizzle reads only declared columns. If drizzle-kit push is run later, it will drop the column.

**Clean devices route:**
In `src/app/api/devices/route.ts` POST handler: Remove `mqttTopicPrefix` from the destructured body and from the `newPlug` object. The DB insert no longer needs it since the column was removed from the schema.

**Remove mqtt package (per D-11):**
In `package.json`: Remove `"mqtt": "^5.15.1"` from dependencies.

**Delete MQTT config rows from DB (per D-08):**
This will happen naturally -- the config table rows (mqtt.host, mqtt.port, mqtt.username, mqtt.password) will remain in SQLite but nothing reads them. No runtime migration needed. If the user runs drizzle-kit push, old data stays but is harmless.

After all changes, run `pnpm install` to update the lockfile (mqtt removal).
  </action>
  <verify>
    <automated>cd /Users/hulki/codex/charging-master && grep -rci "mqtt" src/modules/mqtt/ 2>/dev/null || echo "mqtt module deleted: OK" && grep -c "mqtt" server.ts && grep -c "mqtt" src/types/global.d.ts && grep -ci "mqtt" src/app/settings/page.tsx && grep -ci "mqtt" src/components/layout/sidebar.tsx && grep "mqtt" package.json || echo "mqtt not in package.json: OK"</automated>
  </verify>
  <acceptance_criteria>
    - Directory src/modules/mqtt/ does not exist (ls returns error)
    - Directory src/app/api/mqtt/ does not exist (ls returns error)
    - File src/components/settings/mqtt-settings.tsx does not exist
    - server.ts contains NO string "mqtt" (case-insensitive grep -ci "mqtt" returns 0)
    - server.ts contains NO string "MqttService" or "__mqttService" or "__discoveredDevices"
    - server.ts still contains "HttpPollingService" and "EventBus" and "ChargeMonitor"
    - src/types/global.d.ts contains NO string "mqtt" or "MqttService" or "DiscoveredDevice"
    - src/types/global.d.ts still contains "__httpPollingService" and "__eventBus" and "__chargeMonitor"
    - src/app/settings/page.tsx contains NO string "mqtt" or "MqttSettings"
    - src/app/settings/page.tsx still contains "PushoverSettings"
    - src/components/layout/sidebar.tsx contains NO string "mqtt" (case-insensitive)
    - src/db/schema.ts contains NO string "mqttTopicPrefix" or "mqtt_topic_prefix"
    - src/app/api/devices/route.ts contains NO string "mqttTopicPrefix"
    - package.json contains NO string "mqtt" in dependencies
    - pnpm install succeeds without mqtt package
  </acceptance_criteria>
  <done>All MQTT code, dependencies, UI components, API routes, globals, and schema references are completely removed from the codebase</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify app starts and runs without MQTT</name>
  <files>none -- verification only</files>
  <action>Human verification of the complete MQTT removal and HTTP discovery feature. No code changes in this task.</action>
  <what-built>Complete MQTT removal -- the app should boot and function using only HTTP communication with Shelly devices. Discovery uses HTTP subnet scanning. Settings page shows only Pushover. Sidebar has no MQTT indicator.</what-built>
  <how-to-verify>
    1. Run `pnpm dev` -- app should start without errors, no MQTT-related messages in console
    2. Open http://localhost:3000 -- dashboard loads normally
    3. Navigate to /devices -- "Geraete suchen" button visible, click it to scan
    4. Navigate to /settings -- only Pushover section visible, no MQTT section
    5. Check sidebar -- no "MQTT Verbunden/Getrennt" indicator at the bottom
    6. Try adding a device manually without IP address -- should show error
    7. Verify `grep -ri "mqtt" src/` returns only comments in relay-http.ts and http-polling-service.ts (those say "no MQTT dependency" which is fine)
  </how-to-verify>
  <verify>
    <automated>cd /Users/hulki/codex/charging-master && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>App builds and starts without MQTT, all pages render correctly, discovery works via HTTP scan</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Settings API | User configures Pushover (no more MQTT config surface) |
| server.ts -> Shelly devices | HTTP-only communication, no MQTT broker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | R (Repudiation) | MQTT config removal | accept | Old MQTT config rows remain in SQLite config table as historical data. No security impact. |
| T-06-07 | E (Elevation of Privilege) | Attack surface reduction | mitigate | Removing mqtt.js package and MQTT broker connection eliminates an entire network protocol surface. This is a security improvement. |
| T-06-08 | T (Tampering) | schema.ts mqttTopicPrefix removal | accept | Column removed from Drizzle schema but may persist in SQLite file. Old data is inert -- nothing reads it. |
</threat_model>

<verification>
1. `pnpm dev` starts without errors
2. `grep -ri "mqtt" src/` returns only "no MQTT dependency" comments in HTTP modules
3. `grep "mqtt" package.json` returns no matches
4. `ls src/modules/mqtt/` returns "No such file or directory"
5. `ls src/app/api/mqtt/` returns "No such file or directory"
6. Dashboard, devices, settings, sidebar all render correctly
7. Device discovery works via HTTP scan
8. Device registration requires IP address
</verification>

<success_criteria>
- App starts and runs without any MQTT broker or mqtt.js package
- Zero MQTT references in server.ts, global.d.ts, settings page, sidebar
- src/modules/mqtt/ and src/app/api/mqtt/ directories deleted
- mqtt-settings.tsx component deleted
- mqtt package removed from package.json
- mqttTopicPrefix removed from schema and device route
- Settings page shows only Pushover configuration
- Sidebar shows no MQTT connection status
</success_criteria>

<output>
After completion, create `.planning/phases/06-device-discovery-mqtt-removal/06-02-SUMMARY.md`
</output>
