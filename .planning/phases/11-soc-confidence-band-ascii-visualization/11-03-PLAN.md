---
phase: 11-soc-confidence-band-ascii-visualization
plan: 03
type: execute
wave: 3
depends_on: [11-01, 11-02]
files_modified:
  - src/modules/charging/soc-band-ascii.ts
  - src/modules/charging/soc-band-ascii.test.ts
  - src/modules/notifications/pushover-client.ts
  - src/modules/notifications/pushover-client.test.ts
  - src/modules/notifications/notification-service.ts
  - src/modules/notifications/notification-service.test.ts
  - src/modules/charging/charge-monitor.ts
  - src/modules/charging/charge-monitor.test.ts
autonomous: true
requirements: [SOCB-04, SOCB-05]
must_haves:
  truths:
    - "renderSocBandAscii produces a deterministic monospace bar for any {socMin, socMax, socBest, targetSoc, width, mode}"
    - "≥ 6 snapshot tests lock the renderer output: full uncertainty, narrow band, exact-target, band-crossing-target, zero-width-collapse, target at edge — output shape is LOCKED to 3 lines (scale row + bar row + markers row), no tick-header alternatives"
    - "Pushover mode uses ASCII-only glyphs (#, =, ., ^, T) — no Unicode in the lock-screen payload"
    - "Unicode mode uses richer glyphs (▓ ▒ ░ ↑ ▲) for dashboard + server logs"
    - "sendPushover forwards an optional monospace: 1 form field to the Pushover API"
    - "NotificationService attaches the ASCII bar to matched + complete messages and sends with monospace=1, via a single locked decision: buildMatchedMessage/buildCompleteMessage return type is extended with optional monospace?: 0 | 1 field"
    - "fireAnomalyNotification (charge-monitor.ts) attaches the bar with monospace=1"
    - "captureEventContext returns socAsciiBar as a non-empty string when band fields are present (W1 regression guard)"
  artifacts:
    - path: "src/modules/charging/soc-band-ascii.ts"
      provides: "Pure renderSocBandAscii function"
      contains: "renderSocBandAscii"
    - path: "src/modules/charging/soc-band-ascii.test.ts"
      provides: "≥ 6 snapshot tests"
    - path: "src/modules/notifications/pushover-client.ts"
      provides: "PushoverMessage with optional monospace field"
      contains: "monospace"
    - path: "src/modules/notifications/notification-service.ts"
      provides: "ASCII bar embedded in matched + complete messages, monospace field threaded through buildMessage return type"
      contains: "renderSocBandAscii"
  key_links:
    - from: "src/modules/notifications/notification-service.ts"
      to: "src/modules/charging/soc-band-ascii.ts"
      via: "renderSocBandAscii({ ... mode: 'pushover' })"
      pattern: "renderSocBandAscii\\("
    - from: "src/modules/notifications/notification-service.ts"
      to: "src/modules/notifications/pushover-client.ts"
      via: "sendPushover({ ..., monospace: 1 })"
      pattern: "monospace:\\s*1"
    - from: "src/modules/charging/charge-monitor.ts"
      to: "src/modules/charging/soc-band-ascii.ts"
      via: "renderSocBandAscii in fireAnomalyNotification + captureEventContext"
      pattern: "renderSocBandAscii"
---

<objective>
Build the pure ASCII renderer that turns `{socMin, socMax, socBest, targetSoc, width, mode}` into a deterministic monospace bar, lock its output with ≥ 6 snapshot tests, then thread it through every Pushover code-path: extend `PushoverMessage` with an optional `monospace` field, embed the rendered bar in `NotificationService.buildMatchedMessage` and `buildCompleteMessage`, and embed it in `charge-monitor.ts:fireAnomalyNotification` + `captureEventContext`. Pushover gets ASCII-only glyphs (lock-screen safety per RESEARCH.md Pitfall 3); a `'unicode'` mode is provided for dashboard + server logs.

Renderer output shape is LOCKED (per B5): exactly 3 lines — `scaleRow\nbarRow\nmarkersRow`. The scale row carries the percent labels via `|`/`+`/`-` marker glyphs at the 0/10/.../100 columns; there is NO separate labeled tick-header row. This eliminates the width-dependent branching that B5 flagged.

Purpose: A pure-function renderer is testable via snapshots; the dual-mode API gives Pushover a glyph set guaranteed to render on iOS/Android notification UI while still letting the dashboard use prettier Unicode. Without the renderer wired into all three notification call-sites, the user's phone never sees the band.

Output: A new `soc-band-ascii` module + snapshot tests; an additive `monospace` flag on the Pushover client; bar-embedded messages in NotificationService and the anomaly path; a populated `socAsciiBar` field on every ChargeStateEvent captured via captureEventContext.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-soc-confidence-band-ascii-visualization/CONTEXT.md
@.planning/phases/11-soc-confidence-band-ascii-visualization/11-RESEARCH.md
@.planning/phases/11-soc-confidence-band-ascii-visualization/11-01-SUMMARY.md
@.planning/phases/11-soc-confidence-band-ascii-visualization/11-02-SUMMARY.md

@src/modules/notifications/pushover-client.ts
@src/modules/notifications/notification-service.ts
@src/modules/notifications/notification-service.test.ts
@src/modules/charging/charge-monitor.ts
@CLAUDE.md

<interfaces>
<!-- Extracted from the codebase. The executor uses these directly. -->

Current PushoverMessage (src/modules/notifications/pushover-client.ts:5-11):
```
export type PushoverMessage = {
  userKey: string;
  apiToken: string;
  title: string;
  message: string;
  priority: number;
};
```

Current sendPushover (pushover-client.ts:17-35):
- POSTs `application/json` to `https://api.pushover.net/1/messages.json`
- Body: `{ token, user, title, message, priority }`
- Returns boolean (true on res.ok, false on exception)

NotificationService message builders (notification-service.ts):
- `buildMatchedMessage` is inline inside `buildMessage` switch case 'matched' at line 171 — lines build a `lines` array then `return { title: 'Ladevorgang erkannt', message: lines.join(' '), priority: 0 }`. The bar should be appended via `lines.push('\n' + renderSocBandAscii({...mode:'pushover'}))`.
- `buildCompleteMessage` is at line 230 — builds a `parts` array, ends with `parts.push('Plug abgeschaltet, sicher zum Abnehmen.');`. Append `parts.push('\n' + renderSocBandAscii({...mode:'pushover'}))` BEFORE the return.
- `handleEvent` at line 116 invokes sendPushover at line 137. This is where the `monospace: 1` flag must thread through — pass `monospace: 1` into the sendPushover call when the rendered bar is part of the message.

fireAnomalyNotification (charge-monitor.ts:470-493):
- Uses raw `fetch()` against the Pushover API with `application/x-www-form-urlencoded`. The minimal-change path is (a) include `monospace: '1'` in the URLSearchParams when the rendered bar is appended. Also extend the message construction at the call site by appending the rendered bar when the plug's session Maps contain band data.

captureEventContext (charge-monitor.ts:905) — per Plan 11-02 — now returns `socAsciiBar: string | undefined`. THIS plan populates that field: in `captureEventContext`, after reading socMin/socMax/socBandConfidence from maps, render the bar via `socAsciiBar = renderSocBandAscii({ socMin, socMax, socBest: machine.socBest, targetSoc: machine.targetSoc, mode: 'unicode' })` so the SSE event carries the live bar for the dashboard fallback.

ChargeStateEvent.socAsciiBar (types.ts) — declared optional in Plan 11-02.

Existing notification-service.test.ts uses vi.mock('@/db/client') and vi.mock('./pushover-client'); reuse this style.

MatchResult band fields are REQUIRED after Plan 11-02 Task 3a's tightening (per B3 hand-off). This plan can rely on `match.socMin/socMax/socBest/bandConfidence` being present without `??` fallbacks for production code (test fixtures may still construct with explicit values).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build pure renderSocBandAscii with dual-mode rendering + ≥ 6 snapshot tests (3-line output shape LOCKED per B5)</name>
  <files>src/modules/charging/soc-band-ascii.ts, src/modules/charging/soc-band-ascii.test.ts</files>
  <read_first>src/modules/charging/dtw.ts, src/modules/notifications/notification-service.ts, .planning/phases/11-soc-confidence-band-ascii-visualization/11-RESEARCH.md</read_first>
  <behavior>
    - Snapshot 1 — full uncertainty: `renderSocBandAscii({ socMin: 0, socMax: 100, socBest: 50, targetSoc: 80, mode: 'pushover' })` produces a 3-line string (scale row, bar row of '=' across full width, markers row with '^' at col matching socBest=50% and 'T' at col matching 80%). Locked via `toMatchInlineSnapshot()`.
    - Snapshot 2 — narrow band: `({ socMin: 45, socMax: 55, socBest: 50, targetSoc: 80, mode: 'pushover' })` — '#' glyphs span cols mapping to 45..55 (best±5), markers have '^' at 50% col and 'T' at 80% col, '.' fills the rest.
    - Snapshot 3 — exact target: `({ socMin: 78, socMax: 82, socBest: 80, targetSoc: 80, mode: 'pushover' })` — '^' and 'T' land in the same column, renderer emits 'X' at that column (special-case overlap glyph).
    - Snapshot 4 — band crossing target: `({ socMin: 75, socMax: 85, socBest: 78, targetSoc: 80, mode: 'pushover' })` — band overlaps target.
    - Snapshot 5 — collapsed point (override case): `({ socMin: 60, socMax: 60, socBest: 60, targetSoc: 80, mode: 'pushover' })` — single '#' column at col 60%, no '=' anywhere.
    - Snapshot 6 — target at left edge: `({ socMin: 5, socMax: 15, socBest: 10, targetSoc: 0, mode: 'pushover' })` — 'T' at col 0.
    - Snapshot 7 — Unicode mode parity: same input as Snapshot 2 but `mode: 'unicode'`, locked separately with '▓' / '▒' / '░' / '↑' / '▲' glyphs and `├ ┼ ─` scale-row glyphs.
    - Test: width parameter — `({ ..., width: 20 })` produces a bar of exactly 20 chars per line.
    - Test: clamping — `({ socMin: -10, socMax: 110, ... })` does not throw; renders as if 0/100 (clamped).
    - Test: degenerate width (1) — does not throw; produces a single-column bar (every row is 1 char).
    - Test: invariants — for any valid input, the bar row's character count equals (input.width ?? 40), the markers row has exactly the same length, and the BAR ROW (line 2 of 3) contains no character outside the allowed bar-glyph set. Per B5, scope the invariant regex to the bar row only: split output by '\n', take index 1 (bar row), assert `barRow.match(/^[#=.]+$/)` for pushover mode and `barRow.match(/^[▓▒░]+$/)` for unicode mode. The scale row (line 0) carries `|`/`+`/`-` (pushover) or `├`/`┼`/`─` (unicode); the markers row (line 2) carries `^`/`T`/`X`/` ` (pushover) or `↑`/`▲`/`X`/` ` (unicode). Test these per-line glyph sets separately so the regex is precise.
  </behavior>
  <action>
    Create `src/modules/charging/soc-band-ascii.ts` exporting:

    - `export type SocBandRenderMode = 'pushover' | 'unicode';`
    - `export interface SocBandRenderInput { socMin: number; socMax: number; socBest: number; targetSoc: number; width?: number; mode?: SocBandRenderMode; }`
    - `export const DEFAULT_BAND_WIDTH = 40;`
    - `export const DEFAULT_BAND_MODE: SocBandRenderMode = 'unicode';`
    - `export function renderSocBandAscii(input: SocBandRenderInput): string`

    Implementation (pure function, no I/O — LOCKED 3-line output shape per B5):
    1. Resolve `width = input.width ?? DEFAULT_BAND_WIDTH`, `mode = input.mode ?? DEFAULT_BAND_MODE`.
    2. Clamp `socMin = Math.max(0, Math.min(100, input.socMin))`, same for socMax, socBest, targetSoc.
    3. Glyph table indexed by mode:
       - `'pushover'`: `{ core: '#', band: '=', outside: '.', best: '^', target: 'T', both: 'X', tickStart: '|', tickMid: '+', tickFill: '-' }`
       - `'unicode'`: `{ core: '▓', band: '▒', outside: '░', best: '↑', target: '▲', both: 'X', tickStart: '├', tickMid: '┼', tickFill: '─' }`
    4. Position helpers:
       - `toCol = (pct: number) => Math.max(0, Math.min(width - 1, Math.round((pct / 100) * (width - 1))));`
       - `const minCol = toCol(socMin); const maxCol = toCol(socMax); const bestCol = toCol(socBest); const targetCol = toCol(targetSoc); const coreLo = toCol(Math.max(0, socBest - 5)); const coreHi = toCol(Math.min(100, socBest + 5));`
    5. Build EXACTLY 3 lines (LOCKED per B5 — no separate labeled tick-header row; the scale row's tick glyphs carry the percent positions without separate width-dependent label branching):
       - Scale row (line 0): array of length width. For each i: if i === 0 || i === width-1 → tickStart. Else if `i % Math.max(1, Math.round((width - 1) / 10)) === 0` → tickMid. Else tickFill. Use `Math.round` so the 10-step ticks land at integer columns.
       - Bar row (line 1): array of length width. For each i: if i >= coreLo && i <= coreHi → core. Else if i >= minCol && i <= maxCol → band. Else outside.
       - Markers row (line 2): array of length width. For each i: if i === bestCol && i === targetCol → both. Else if i === bestCol → best. Else if i === targetCol → target. Else ' '.
    6. Return `${scaleRow}\n${barRow}\n${markersRow}` (three lines separated by single `\n`, no trailing newline).

    Write the `<behavior>` snapshot tests in NEW file `src/modules/charging/soc-band-ascii.test.ts`. Use Vitest's `expect(result).toMatchInlineSnapshot(\`...\`)` so the snapshots live inside the test file and are reviewed during code review (no separate __snapshots__ directory needed). For mode-symmetric snapshots, prefer `toMatchInlineSnapshot()` over file snapshots so a glyph regression is immediately visible in the diff.

    For the invariants test (per B5): split the rendered string by `\n`, take exactly index 1 (the bar row), and assert its regex against the bar-only glyph set (`/^[#=.]+$/` for pushover, `/^[▓▒░]+$/` for unicode). Test the scale row (index 0) and markers row (index 2) with their own narrower regexes — do NOT use one global regex that has to match all glyphs.

    Determinism: avoid Math.random, Date.now, or any non-pure source. Every call with the same input MUST produce byte-identical output.

    Note for Plan 11-04: this module is also imported by the dashboard component (`'unicode'` mode) and by charge-monitor.ts (captureEventContext, `'unicode'` mode for SSE).
  </action>
  <verify>
    <automated>pnpm exec vitest run src/modules/charging/soc-band-ascii.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/modules/charging/soc-band-ascii.ts` succeeds
    - `test -f src/modules/charging/soc-band-ascii.test.ts` succeeds
    - `grep -n "export function renderSocBandAscii" src/modules/charging/soc-band-ascii.ts` returns one match
    - `grep -nE "'pushover'|'unicode'" src/modules/charging/soc-band-ascii.ts | wc -l` returns at least 4 (both modes appear in type, default, and glyph-table dispatch)
    - `grep -cE "toMatchInlineSnapshot" src/modules/charging/soc-band-ascii.test.ts` returns at least 7 (6 main snapshots + Unicode parity)
    - The renderer emits exactly 3 lines (B5 lock): `node -e "const m=require('./src/modules/charging/soc-band-ascii.ts'); ..."` — OR equivalently, the inline snapshots in soc-band-ascii.test.ts contain exactly 2 `\n` characters each (split by `\n` yields length 3). The invariants test in the file asserts this directly.
    - `pnpm exec vitest run src/modules/charging/soc-band-ascii.test.ts` exits 0 with ≥ 10 tests passing
    - Running the same test file twice in succession produces identical snapshot content (determinism check — `pnpm exec vitest run --run src/modules/charging/soc-band-ascii.test.ts && pnpm exec vitest run --run src/modules/charging/soc-band-ascii.test.ts` both pass)
    - The function is pure — `grep -nE "import.*db|fs|fetch|Date\.now|Math\.random" src/modules/charging/soc-band-ascii.ts` returns 0 matches
    - `pnpm exec tsc --noEmit` exits 0
    - Per-line glyph invariants pass (bar row matches `/^[#=.]+$/` for pushover, `/^[▓▒░]+$/` for unicode; scale + markers rows have their own narrow regexes)
  </acceptance_criteria>
  <done>
    The renderer is pure, dual-mode, deterministic, and locked by ≥ 6 inline snapshots (plus the Unicode parity snapshot). Output shape is fixed at exactly 3 lines (B5 closed). Per-line glyph invariants prevent the over-broad-regex issue.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add monospace field to PushoverMessage + sendPushover; embed bar in NotificationService matched/complete + charge-monitor anomaly + captureEventContext</name>
  <files>src/modules/notifications/pushover-client.ts, src/modules/notifications/pushover-client.test.ts, src/modules/notifications/notification-service.ts, src/modules/notifications/notification-service.test.ts, src/modules/charging/charge-monitor.ts, src/modules/charging/charge-monitor.test.ts</files>
  <read_first>src/modules/notifications/pushover-client.ts, src/modules/notifications/notification-service.ts, src/modules/notifications/notification-service.test.ts, src/modules/charging/charge-monitor.ts, src/modules/charging/soc-band-ascii.ts</read_first>
  <behavior>
    - Test (pushover-client.test.ts, NEW): mock global fetch. Call `sendPushover({ userKey, apiToken, title, message, priority: 0, monospace: 1 })`. Assert: fetch was called with JSON body containing `"monospace": 1`. Call again without `monospace` field. Assert: fetch body does NOT contain a monospace key (omitted, not `0`).
    - Test (pushover-client.test.ts): on fetch failure, sendPushover returns false; on res.ok=false (e.g. 429 rate limit), sendPushover returns false; on res.ok=true, returns true. (Regression coverage for the existing function.)
    - Test (notification-service.test.ts, matched message includes bar): mock db to return Pushover credentials + a session with profileId set; emit a ChargeStateEvent of state='matched' with socMin=40 socMax=60 socBest=50 targetSoc=80. Assert: the sendPushover spy was called with a `message` string containing the rendered Pushover-mode bar (assert `message.includes('#') || message.includes('=')` — the bar's glyphs).
    - Test (notification-service.test.ts, matched message has monospace=1): the same call site → sendPushover spy received `{ monospace: 1 }`.
    - Test (notification-service.test.ts, complete message includes bar): emit state='complete' with socMin=78 socMax=82 socBest=80 targetSoc=80 → message contains the bar AND monospace=1.
    - Test (notification-service.test.ts, no bar when band fields missing): emit state='matched' WITHOUT socMin/socMax (undefined). The bar must NOT be appended — message stays in legacy single-line form, monospace is omitted. (Back-compat guard.)
    - Test (charge-monitor.test.ts — extend Plan 11-02's file): fire fireAnomalyNotification with band data on the plug's session maps. Mock global fetch. Assert: the POSTed URLSearchParams body has `monospace=1` AND message contains a rendered bar.
    - Test (W1 regression — captureEventContext returns socAsciiBar as a non-empty string when band fields are present): on an active session whose Maps have socMin=78, socMax=82, machine.socBest=80, machine.targetSoc=80, call `captureEventContext(plugId)`. Assert: `result.socAsciiBar` is `typeof 'string'` AND `result.socAsciiBar.length > 0` AND `result.socAsciiBar.split('\n').length === 3` (matches the locked 3-line shape from Task 1). When band fields are missing on the Maps, `result.socAsciiBar` is `undefined` (legacy fallback).
  </behavior>
  <action>
    **pushover-client.ts:**
    1. Extend `PushoverMessage` type with optional field `monospace?: 0 | 1;` (between `priority: number;` and the closing brace).
    2. In `sendPushover`, change the body builder to: declare `const body: Record<string, unknown> = { token: msg.apiToken, user: msg.userKey, title: msg.title, message: msg.message, priority: msg.priority }; if (msg.monospace) body.monospace = 1;` — only add the field when truthy so legacy callers send identical bodies.
    3. Create `src/modules/notifications/pushover-client.test.ts` with the four `<behavior>` tests above. Use `vi.stubGlobal('fetch', vi.fn().mockResolvedValue({ ok: true }))` and assert `(fetch as Mock).mock.calls[0][1].body` parsed JSON contents.

    **notification-service.ts (W4 — single locked decision, no OR-fork):**

    The locked decision: EXTEND `buildMatchedMessage` and `buildCompleteMessage` return types to include an optional `monospace?: 0 | 1` field on the returned object. When the bar was appended, set `monospace: 1`. `handleEvent` reads `msg.monospace` from the builder result and forwards it into the sendPushover call. NO parallel-path / OR alternative.

    1. Import `renderSocBandAscii` from `@/modules/charging/soc-band-ascii`.
    2. Update the inferred return type of `buildMessage` (and the inline `buildMatchedMessage` / `buildCompleteMessage` constructions inside the switch cases) to be `{ title: string; message: string; priority: number; monospace?: 0 | 1 }`.
    3. In the `matched` case (line 171) inside `buildMessage`:
       - After building `lines`, if `event.socMin != null && event.socMax != null && event.targetSoc != null && event.estimatedSoc != null` → append `lines.push('\n' + renderSocBandAscii({ socMin: event.socMin, socMax: event.socMax, socBest: event.estimatedSoc, targetSoc: event.targetSoc, mode: 'pushover' }))` AND include `monospace: 1` in the returned object. (Use `estimatedSoc` as socBest per the back-compat alias.)
    4. In `buildCompleteMessage` (line 230), same pattern: before the return, append the rendered bar to `parts` if band fields are present, and include `monospace: 1` in the returned object.
    5. In `handleEvent` (line 116), pass `monospace: msg.monospace` into the sendPushover call: `await sendPushover({ userKey: credentials.userKey, apiToken: credentials.apiToken, title: \`[${label}] ${msg.title}\`, message: msg.message, priority: msg.priority, monospace: msg.monospace });`.
    6. Frequency restraint (per CONTEXT.md §3 and SOCB-05): attach the bar ONLY in `matched` and `complete` cases. Do NOT modify the `detecting` builder — that fires repeatedly during detection and would spam. Do NOT modify `error`/`aborted`/`learn_complete`.

    **charge-monitor.ts (fireAnomalyNotification line 470):**
    1. Import `renderSocBandAscii` from `./soc-band-ascii`.
    2. Read band state from the plug's session maps: `const socMin = this.sessionSocMin.get(plugId); const socMax = this.sessionSocMax.get(plugId); const machine = this.machines.get(plugId);`. If those values are all present AND machine has socBest + targetSoc, render the bar: `const bar = renderSocBandAscii({ socMin, socMax, socBest: machine.socBest, targetSoc: machine.targetSoc, mode: 'pushover' }); const message = \`${baseMessage}\\n\\n${bar}\`;` — otherwise use the message unchanged.
    3. Add `monospace: '1'` to the URLSearchParams body in the fetch call when the bar is present.

    **charge-monitor.ts (captureEventContext — populate socAsciiBar):** Plan 11-02's Task 3a left `socAsciiBar` as `undefined`. NOW populate it: in `captureEventContext`, after reading socMin/socMax/socBandConfidence from maps, if band fields exist render the bar — `socAsciiBar: (socMin != null && socMax != null && machine != null) ? renderSocBandAscii({ socMin, socMax, socBest: machine.socBest, targetSoc: machine.targetSoc, mode: 'unicode' }) : undefined`. This is the W1 fix: the post-await complete event will now carry a non-empty `socAsciiBar` instead of silently emitting undefined.

    Tests:
    - EXTEND `src/modules/notifications/notification-service.test.ts` with the new cases. Reuse the vi.mock('./pushover-client') pattern.
    - EXTEND `src/modules/charging/charge-monitor.test.ts` (from Plan 11-02) with:
      • The fireAnomalyNotification test (mock fetch, assert monospace=1 + bar in body).
      • The W1 regression test: assert `captureEventContext(...).socAsciiBar` is a non-empty 3-line string when band fields are present on the Maps, and `undefined` otherwise.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/modules/notifications src/modules/charging/charge-monitor.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "monospace?: 0 | 1" src/modules/notifications/pushover-client.ts` returns one match
    - `grep -nE "if \(msg\.monospace\)" src/modules/notifications/pushover-client.ts` returns one match
    - `grep -n "renderSocBandAscii" src/modules/notifications/notification-service.ts` returns at least two matches (matched + complete)
    - `grep -n "renderSocBandAscii" src/modules/charging/charge-monitor.ts` returns at least two matches (fireAnomalyNotification + captureEventContext)
    - `grep -nE "monospace:\s*1|monospace:\s*'1'" src/modules/charging/charge-monitor.ts` returns at least one match (anomaly path)
    - `grep -n "monospace:\s*1" src/modules/notifications/notification-service.ts` returns at least one match (set in builder return)
    - `grep -n "monospace: msg.monospace\|monospace: result.monospace" src/modules/notifications/notification-service.ts` returns one match (handleEvent forwards the flag — W4 single path)
    - `pnpm exec vitest run src/modules/notifications/pushover-client.test.ts` exits 0 with ≥ 4 tests passing
    - `pnpm exec vitest run src/modules/notifications/notification-service.test.ts` exits 0 with all existing tests AND the four new cases passing
    - `pnpm exec vitest run src/modules/charging/charge-monitor.test.ts` exits 0 — anomaly test + W1 captureEventContext regression test PASS
    - Manual diff: legacy callers of sendPushover (notifications without bands, e.g. detecting/error/learn_complete) MUST still work — their behavior is unchanged because monospace is opt-in.
    - `pnpm exec tsc --noEmit` exits 0
    - `pnpm exec vitest run` (full suite) exits 0
  </acceptance_criteria>
  <done>
    The Pushover client carries the monospace flag opt-in, the notification service attaches the bar to matched + complete via a SINGLE locked code path (W4 closed: buildMessage return type extended with optional monospace field — no OR-fork), the anomaly path attaches the bar too, and captureEventContext now returns socAsciiBar as a non-empty 3-line string when band fields are present (W1 closed).
  </done>
</task>

</tasks>

<verification>
- `pnpm exec vitest run` exits 0 with ≥ 71 tests (Phase 11-01 + 11-02 baselines + ≥ 10 renderer snapshots + 4 pushover-client tests + ≥ 4 new notification-service cases + extended charge-monitor anomaly + W1 captureEventContext regression).
- `pnpm exec tsc --noEmit` exits 0.
- `grep -rn "renderSocBandAscii" src/ | wc -l` returns ≥ 5 (renderer + 2 consumer call sites in notifications + 2 in charge-monitor).
- Manual semi-deterministic check: the snapshot files (inline) for Snapshots 1-7 should be visually inspectable — every glyph in the BAR row (line index 1) is one of the bar-only glyphs per mode; scale + markers rows have their own glyph sets.
- The W1 regression test in charge-monitor.test.ts is identifiable in `pnpm exec vitest run --reporter=verbose` and not skipped.
</verification>

<success_criteria>
- SOCB-04 satisfied: pure `renderSocBandAscii` with ≥ 6 snapshot tests covering CONTEXT.md's representative inputs; output shape locked at 3 lines (B5 closed).
- SOCB-05 satisfied: NotificationService attaches the bar to matched + complete with `monospace=1`; anomaly path attaches the bar too. State frequency limited to matched/complete/anomalie (no per-Wh spam).
- B5 closed: renderer output is exactly 3 lines (scale + bar + markers) — no separate labeled tick-header, no width-dependent branching, no "OR" alternatives in the action. Invariant regex scoped per-line.
- W1 closed: captureEventContext now returns socAsciiBar as a non-empty 3-line string when band fields are present; regression test in charge-monitor.test.ts enforces this. The post-await complete event will not silently emit `socAsciiBar: undefined`.
- W4 closed: monospace flag threads through buildMessage return type — single locked code path, no parallel "OR" alternative.
- The 2640873 bug-class regression is locked: complete event's socAsciiBar comes through captureEventContext's pre-await snapshot.
- Legacy code paths are untouched — detecting, error, aborted, learn_complete messages are byte-identical to pre-Phase-11.
</success_criteria>

<output>
After completion, create `.planning/phases/11-soc-confidence-band-ascii-visualization/11-03-SUMMARY.md` recording: the final glyph tables for both modes, the locked 3-line output shape (with per-line glyph sets), the snapshot file locations (inline), the three Pushover call sites that now carry monospace=1, and the captureEventContext socAsciiBar wiring so Plan 11-04 can pull it from the SSE payload directly.
</output>
</content>
</invoke>