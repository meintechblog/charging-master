---
phase: 03-charge-intelligence
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - src/app/profiles/page.tsx
  - src/app/profiles/[id]/page.tsx
  - src/app/profiles/learn/page.tsx
  - src/components/charging/learn-wizard.tsx
  - src/components/charging/soc-buttons.tsx
  - src/components/charging/profile-form.tsx
  - src/components/layout/sidebar.tsx
  - src/hooks/use-charge-stream.ts
autonomous: false
requirements: [PROF-07, VIZL-03]

must_haves:
  truths:
    - "User can see all device profiles listed on /profiles page with name, target SOC, and curve status"
    - "User can create a new profile via form with all D-30 attributes"
    - "User can view profile detail with reference curve chart (if learned)"
    - "User can edit profile attributes and delete profiles"
    - "User can start learn mode via step-by-step wizard (D-25)"
    - "During learning, user sees live chart with Wh counter, elapsed time, and 'Ladevorgang aktiv' badge (D-26)"
    - "User can set target SOC via 10%-step buttons (D-31)"
    - "Sidebar includes /profiles link (D-29)"
    - "Reference curve displayed as dashed gray line overlay on PowerChart (VIZL-03)"
  artifacts:
    - path: "src/app/profiles/page.tsx"
      provides: "Profile list page with create button"
      min_lines: 40
    - path: "src/app/profiles/[id]/page.tsx"
      provides: "Profile detail with reference curve chart and edit form"
      min_lines: 60
    - path: "src/app/profiles/learn/page.tsx"
      provides: "Learn mode wizard page"
      min_lines: 50
    - path: "src/components/charging/learn-wizard.tsx"
      provides: "Step-by-step learn mode wizard component"
      min_lines: 80
    - path: "src/components/charging/soc-buttons.tsx"
      provides: "10%-step SOC target selector"
      min_lines: 20
    - path: "src/hooks/use-charge-stream.ts"
      provides: "Hook for SSE charge state events"
      exports: ["useChargeStream"]
  key_links:
    - from: "src/components/charging/learn-wizard.tsx"
      to: "/api/charging/learn/start"
      via: "fetch POST to start learn mode"
      pattern: "api/charging/learn/start"
    - from: "src/app/profiles/[id]/page.tsx"
      to: "/api/profiles/[id]/curve"
      via: "fetch GET for reference curve data"
      pattern: "api/profiles.*curve"
    - from: "src/components/charts/power-chart.tsx"
      to: "referenceData prop"
      via: "second ECharts series for overlay"
      pattern: "referenceData|Referenz"
---

<objective>
Profile management UI pages and learn mode wizard with live recording visualization.

Purpose: Deliver the user-facing experience for device profile management (D-29..D-32) and the learn mode wizard (D-25..D-28). Includes the reference curve overlay on PowerChart (VIZL-03) and the SSE hook for charge state events that the active charging UI (Plan 05) also needs.
Output: 3 pages (/profiles, /profiles/[id], /profiles/learn), wizard component, SOC buttons, charge stream hook, PowerChart overlay extension.
</objective>

<execution_context>
@/Users/hulki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hulki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-charge-intelligence/03-CONTEXT.md
@.planning/phases/03-charge-intelligence/03-02-SUMMARY.md
@.planning/phases/03-charge-intelligence/03-03-SUMMARY.md
@src/components/charts/power-chart.tsx
@src/components/layout/sidebar.tsx
@src/hooks/use-power-stream.ts
@src/app/api/profiles/route.ts
@src/app/api/charging/learn/start/route.ts

<interfaces>
From src/app/api/profiles/route.ts (Plan 02):
```typescript
// GET returns: Array<{ id, name, description, modelName, targetSoc, hasCurve, totalEnergyWh, createdAt }>
// POST accepts: { name, description?, modelName?, purchaseDate?, estimatedCycles?, targetSoc? }
```

From src/app/api/profiles/[id]/curve/route.ts (Plan 02):
```typescript
// GET returns: Array<{ offsetSeconds, apower, voltage, current, cumulativeWh }>
```

From src/app/api/charging/learn/start/route.ts (Plan 03):
```typescript
// POST accepts: { plugId, profileId }
// Returns: { sessionId, plugId, profileId, state: 'learning' }
```

From src/app/api/charging/learn/status/route.ts (Plan 03):
```typescript
// GET returns: Array<{ sessionId, plugId, profileId, state, startedAt, duration, readingCount, latestPower, cumulativeWh }>
```

From src/app/api/charging/learn/stop/route.ts (Plan 03):
```typescript
// POST accepts: { plugId, action: 'save' | 'discard' }
```

From src/components/charts/power-chart.tsx:
```typescript
type PowerChartProps = {
  plugId: string;
  initialWindow?: WindowKey;
  initialData?: Array<[number, number]>;
  onWindowChange?: (key: WindowKey) => void;
  height?: string;
};
```

From src/components/layout/sidebar.tsx:
```typescript
const NAV_ITEMS = [
  { href: '/', label: 'Dashboard' },
  { href: '/devices', label: 'Geraete' },
  { href: '/settings', label: 'Einstellungen' },
  { href: '/history', label: 'Verlauf', disabled: true },
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Charge stream hook + PowerChart reference overlay + SOC buttons + profile form</name>
  <files>
    src/hooks/use-charge-stream.ts,
    src/components/charts/power-chart.tsx,
    src/components/charging/soc-buttons.tsx,
    src/components/charging/profile-form.tsx
  </files>
  <read_first>
    src/hooks/use-power-stream.ts,
    src/components/charts/power-chart.tsx,
    .planning/phases/03-charge-intelligence/03-RESEARCH.md
  </read_first>
  <action>
    **1. useChargeStream hook (src/hooks/use-charge-stream.ts):**
    Follow the exact same pattern as usePowerStream. Listen to the shared singleton EventSource (from use-power-stream.ts module-level variable). Add an event listener for the named SSE event `charge`. Parse JSON data as ChargeStateEvent. Call the provided callback with the event. Filter by plugId if provided, or receive all if plugId is '*'.

    Import the shared EventSource setup from use-power-stream.ts. If it doesn't export the ensureEventSource / refCount logic, extract the shared EventSource management into a separate file `src/hooks/sse-shared.ts` and have both hooks import from there.

    **2. Extend PowerChart (src/components/charts/power-chart.tsx):**
    Add optional prop: `referenceData?: Array<[number, number]>` (timestamp-aligned [time, watts] pairs).
    Per VIZL-03: When referenceData is provided, add a second series to the ECharts option:
    ```
    {
      name: 'Referenz',
      type: 'line',
      smooth: true,
      showSymbol: false,
      lineStyle: { color: '#6b7280', width: 1.5, type: 'dashed' },
      areaStyle: undefined,
      data: referenceData,
      z: 0,
    }
    ```
    Add a legend when referenceData is present: `legend: { data: ['Aktuell', 'Referenz'], textStyle: { color: '#a3a3a3' } }`.
    Update the tooltip to handle multiple series (show both values if both present).
    Name the existing live series 'Aktuell'.

    **3. SocButtons component (src/components/charging/soc-buttons.tsx):**
    Per D-31: Row of 10 buttons (10%, 20%, ... 100%). Props: `value: number, onChange: (soc: number) => void, disabled?: boolean`.
    Active button uses blue-500 highlight (same as chart accent). Others use neutral-800 background.
    Compact styling: `px-2 py-1 text-xs rounded`. Each button shows just the number (e.g., "80%").

    **4. ProfileForm component (src/components/charging/profile-form.tsx):**
    Per D-30: Reusable form for creating and editing profiles.
    Fields: Name (text, required), Beschreibung (textarea, optional), Modellbezeichnung (text, optional), Kaufdatum (date input, optional), Geschaetzte Ladezyklen (number input, optional).
    Props: `initialValues?: Partial<ProfileFormValues>, onSubmit: (values: ProfileFormValues) => void, submitLabel?: string, disabled?: boolean`.
    Uses standard controlled form state with useState. Dark theme inputs (bg-neutral-800, border-neutral-700, text-neutral-100).
  </action>
  <verify>
    <automated>pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "useChargeStream" src/hooks/use-charge-stream.ts
    - grep -q "referenceData" src/components/charts/power-chart.tsx
    - grep -q "Referenz" src/components/charts/power-chart.tsx
    - grep -q "SocButtons" src/components/charging/soc-buttons.tsx
    - grep -q "ProfileForm" src/components/charging/profile-form.tsx
  </acceptance_criteria>
  <done>Charge stream hook receives SSE charge events. PowerChart shows reference curve as dashed gray overlay. SOC buttons allow 10%-step selection. Profile form handles all D-30 attributes.</done>
</task>

<task type="auto">
  <name>Task 2: Profile pages + learn wizard + sidebar nav</name>
  <files>
    src/app/profiles/page.tsx,
    src/app/profiles/[id]/page.tsx,
    src/app/profiles/learn/page.tsx,
    src/components/charging/learn-wizard.tsx,
    src/components/layout/sidebar.tsx
  </files>
  <read_first>
    src/components/charging/soc-buttons.tsx,
    src/components/charging/profile-form.tsx,
    src/components/charts/power-chart.tsx,
    src/hooks/use-charge-stream.ts,
    src/components/layout/sidebar.tsx,
    src/app/devices/page.tsx
  </read_first>
  <action>
    **1. Update sidebar (src/components/layout/sidebar.tsx):**
    Per D-29: Add `{ href: '/profiles', label: 'Profile' }` to NAV_ITEMS, after 'Geraete' and before 'Einstellungen'.

    **2. Profiles list page (src/app/profiles/page.tsx):**
    Server component. Fetch profiles from /api/profiles (or direct DB query since server component).
    Display as a list/table with columns: Name, Modell, Ziel-SOC, Referenzkurve (checkmark or dash), Erstellt.
    "Neues Profil" button links to /profiles/learn (starts with wizard to create profile + learn).
    Each row links to /profiles/[id].
    Empty state: "Noch keine Profile angelegt. Starte mit dem Anlernen eines Geraets."
    Dark theme: bg-neutral-900 cards, neutral-800 table rows, neutral-100 text.

    **3. Profile detail page (src/app/profiles/[id]/page.tsx):**
    'use client' page (needs interactivity for edit/delete/SOC buttons).
    Fetch profile data and reference curve on mount.
    Sections:
    - Header: Profile name, edit/delete buttons
    - Attributes: Display all D-30 fields, editable via ProfileForm (toggle edit mode)
    - Ziel-SOC: SocButtons component, saves on change via PUT /api/profiles/[id]
    - Referenzkurve: If curve exists, show PowerChart with curve data as static series (not live). Show metadata (Dauer, Energie, Datenpunkte). "Neu anlernen" button links to /profiles/learn?profileId=[id]&plugId=... (per D-32 re-learn action).
    - Loeschen: Delete button with confirmation dialog. On confirm, DELETE /api/profiles/[id], redirect to /profiles.

    **4. Learn wizard component (src/components/charging/learn-wizard.tsx):**
    Per D-25: Step-by-step wizard with 4 steps:
    - Step 1: "Geraetename" -- text input for device name (auto-fills if profileId from URL). Shows ProfileForm fields.
    - Step 2: "Shelly Plug auswaehlen" -- fetch plugs from /api/devices, show as selectable cards. Selected plug highlighted.
    - Step 3: "Hinweis" -- Info card: "Akku moeglichst leer? Fuer ein genaues Profil sollte der Akku vor dem Anlernen moeglichst leer sein." Checkbox: "Ja, Akku ist leer" to enable Start button.
    - Step 4: "Aufnahme" -- Live recording view:
      - POST /api/charging/learn/start to begin
      - PowerChart component showing live data for selected plug (existing SSE stream)
      - Per D-26: Wh counter (from learn status polling or SSE), elapsed time (computed from startedAt), "Ladevorgang aktiv" badge (green pulsing dot)
      - Poll /api/charging/learn/status every 5 seconds for readingCount, cumulativeWh, latestPower
      - Per D-27: When learn status returns state='learn_complete', show confirmation dialog: "Ladevorgang abgeschlossen -- Profil speichern?" with "Speichern" and "Verwerfen" buttons
      - On save: POST /api/charging/learn/stop with action='save'. Show success message, redirect to /profiles/[id].
      - On discard: POST /api/charging/learn/stop with action='discard'. Redirect to /profiles.

    Navigation: Back button on each step. Step indicator (1/4, 2/4, etc.) at top.

    **5. Learn page (src/app/profiles/learn/page.tsx):**
    Thin wrapper page that renders LearnWizard component. Reads `profileId` and `plugId` from URL search params (for re-learn flow from profile detail page).
    Per D-28: On mount, check /api/charging/learn/status. If an active learning session exists, skip to Step 4 (resume recording view). This handles browser-close-and-reopen scenario.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Profile" src/components/layout/sidebar.tsx
    - grep -q "profiles" src/components/layout/sidebar.tsx
    - test -f src/app/profiles/page.tsx
    - test -f src/app/profiles/[id]/page.tsx
    - test -f src/app/profiles/learn/page.tsx
    - grep -q "LearnWizard" src/components/charging/learn-wizard.tsx
    - grep -q "learn/start" src/components/charging/learn-wizard.tsx
    - grep -q "learn/status" src/components/charging/learn-wizard.tsx
    - grep -q "learn/stop" src/components/charging/learn-wizard.tsx
  </acceptance_criteria>
  <done>Profile management UI complete. Users can list, create, view, edit, delete profiles. Learn mode wizard guides through 4 steps. Live recording shows chart, Wh counter, elapsed time. Auto-detection of charge complete triggers save dialog. Browser-close resilient via status check on mount. Sidebar includes /profiles link.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify profile pages and learn wizard</name>
  <files>src/app/profiles/page.tsx, src/app/profiles/learn/page.tsx, src/components/layout/sidebar.tsx</files>
  <action>
    Human verification of profile management UI and learn mode wizard.
    What was built: Profile list page, profile detail page, learn mode wizard, SOC buttons, sidebar navigation link, PowerChart reference curve overlay.
    Steps to verify:
    1. Open http://localhost:3000 -- verify "Profile" appears in sidebar navigation
    2. Click "Profile" -- should show empty state or profile list at /profiles
    3. Click "Neues Profil" -- should open learn wizard at /profiles/learn
    4. Step through wizard: enter name, select a plug (if available), see hint step
    5. If a Shelly plug is connected and charging, start recording and verify live chart, Wh counter, elapsed time, "Ladevorgang aktiv" badge
    6. Navigate to a profile detail page (if one exists) -- verify reference curve chart, SOC buttons, edit form, delete button
    7. Verify all pages follow dark theme (neutral-900 background, blue-500 accents)
    Resume signal: Type "approved" or describe issues.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - User confirms profile pages and learn wizard work correctly
  </acceptance_criteria>
  <done>User has verified profile management UI and learn mode wizard are functional and visually correct.</done>
</task>

</tasks>

<verification>
- `pnpm tsc --noEmit` passes
- /profiles page renders profile list
- /profiles/[id] shows profile detail with curve chart
- /profiles/learn wizard steps through 4 stages
- PowerChart shows reference overlay when referenceData provided
- Sidebar includes Profile link
</verification>

<success_criteria>
- User can manage profiles end-to-end (create, view, edit, delete)
- Learn mode wizard guides through the 4 D-25 steps
- Live recording shows chart, Wh counter, elapsed time per D-26
- Charge-complete auto-detection triggers save dialog per D-27
- Browser-close resilient learn mode per D-28
- Target SOC configurable via 10%-step buttons per D-31
- Reference curve overlay on PowerChart per VIZL-03
</success_criteria>

<output>
After completion, create `.planning/phases/03-charge-intelligence/03-04-SUMMARY.md`
</output>
