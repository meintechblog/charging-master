---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/api/settings/route.ts
  - src/app/api/mqtt/test/route.ts
  - src/app/settings/page.tsx
  - src/components/settings/mqtt-settings.tsx
  - src/components/settings/pushover-settings.tsx
  - src/components/settings/settings-section.tsx
autonomous: true
requirements:
  - SETT-01
  - SETT-02
must_haves:
  truths:
    - "User can configure MQTT broker settings (host, port, username, password) and they persist across restarts"
    - "User can configure Pushover settings (user key, API token) and they persist across restarts"
    - "Every settings change auto-saves immediately without a save button"
    - "User can test MQTT broker connectivity and see inline success/failure result"
  artifacts:
    - path: "src/app/settings/page.tsx"
      provides: "Settings page with MQTT and Pushover sections"
      contains: "MqttSettings"
    - path: "src/app/api/settings/route.ts"
      provides: "Settings CRUD API (GET all, PUT single key)"
      exports: ["GET", "PUT"]
    - path: "src/app/api/mqtt/test/route.ts"
      provides: "MQTT connection test endpoint"
      exports: ["POST"]
  key_links:
    - from: "src/components/settings/mqtt-settings.tsx"
      to: "/api/settings"
      via: "fetch PUT on input change (debounced)"
      pattern: "fetch.*api/settings.*PUT"
    - from: "src/components/settings/mqtt-settings.tsx"
      to: "/api/mqtt/test"
      via: "fetch POST on test button click"
      pattern: "fetch.*api/mqtt/test"
    - from: "src/app/api/settings/route.ts"
      to: "src/db/schema.ts"
      via: "Drizzle insert/select on config table"
      pattern: "db\\.insert\\(config\\)|db\\.select.*from\\(config\\)"
---

<objective>
Build the Settings page with MQTT broker configuration, Pushover credentials, and auto-save functionality. Includes MQTT connection test button and API routes for settings persistence.

Purpose: Users need to configure their MQTT broker before any Shelly data can flow (SETT-01), and Pushover credentials for future notifications (SETT-02). Per D-05, settings auto-save immediately.
Output: Fully functional /settings page with MQTT and Pushover sections, backed by API routes and SQLite config table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

Depends on Plan 01 for: src/db/client.ts (db export), src/db/schema.ts (config table), src/modules/mqtt/mqtt-service.ts (testConnection method), src/types/global.d.ts (globalThis types).

<interfaces>
From src/db/schema.ts (created in Plan 01):
```typescript
export const config = sqliteTable('config', {
  key: text('key').primaryKey(),
  value: text('value').notNull(),
  updatedAt: integer('updated_at').notNull(),
});
```

From src/modules/mqtt/mqtt-service.ts (created in Plan 01):
```typescript
export class MqttService {
  async testConnection(brokerUrl: string, options?: IClientOptions): Promise<boolean>;
  async connect(brokerUrl: string, options?: IClientOptions): Promise<void>;
  isConnected(): boolean;
}
```

globalThis.__mqttService: MqttService (set in server.ts)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settings API routes (CRUD + MQTT test)</name>
  <files>
    src/app/api/settings/route.ts,
    src/app/api/mqtt/test/route.ts
  </files>
  <read_first>
    src/db/client.ts (db export, import path)
    src/db/schema.ts (config table definition)
    src/modules/mqtt/mqtt-service.ts (testConnection method signature)
    src/types/global.d.ts (globalThis.__mqttService type)
    .planning/phases/01-foundation/01-RESEARCH.md (Settings API Route code example)
  </read_first>
  <action>
    1. **src/app/api/settings/route.ts**: Per RESEARCH.md Settings API example and D-05 auto-save:
       - `GET`: Select all rows from `config` table, return as `{ [key]: value }` object via `Response.json(settings)`.
       - `PUT`: Accept JSON body `{ key: string, value: string }`. Validate key is a non-empty string. Upsert into config table using `db.insert(config).values({ key, value, updatedAt: Date.now() }).onConflictDoUpdate({ target: config.key, set: { value, updatedAt: Date.now() } })`. Return `Response.json({ ok: true })`. On invalid input, return `Response.json({ error: "invalid_input" }, { status: 400 })`.

    2. **src/app/api/mqtt/test/route.ts**: Per D-06, MQTT connection test button:
       - `export const runtime = 'nodejs'`
       - `POST`: Accept JSON body `{ host: string, port?: number, username?: string, password?: string }`. Validate host is non-empty. Build broker URL as `mqtt://<host>:<port || 1883>`. Build options object with username/password if provided. Call `globalThis.__mqttService.testConnection(brokerUrl, options)`. Return `Response.json({ success: true/false })`. On error, return `Response.json({ success: false, error: message }, { status: 500 })`.
  </action>
  <verify>
    <automated>cd /Users/hulki/codex/charging-master && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/settings/route.ts contains `export async function GET` and `export async function PUT`
    - src/app/api/settings/route.ts contains `onConflictDoUpdate` and `config.key`
    - src/app/api/mqtt/test/route.ts contains `export async function POST` and `testConnection`
    - src/app/api/mqtt/test/route.ts contains `export const runtime = 'nodejs'`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Settings GET/PUT API works for key-value config persistence, MQTT test endpoint validates broker connectivity</done>
</task>

<task type="auto">
  <name>Task 2: Settings page with MQTT and Pushover sections (per D-04, D-05, D-06)</name>
  <files>
    src/app/settings/page.tsx,
    src/components/settings/settings-section.tsx,
    src/components/settings/mqtt-settings.tsx,
    src/components/settings/pushover-settings.tsx
  </files>
  <read_first>
    src/app/layout.tsx (root layout structure, styling approach)
    src/app/api/settings/route.ts (API contract from Task 1)
    src/app/api/mqtt/test/route.ts (test endpoint contract from Task 1)
    .planning/phases/01-foundation/01-CONTEXT.md (D-03 dark theme, D-04 settings sections, D-05 auto-save, D-06 test button)
    .planning/phases/01-foundation/01-RESEARCH.md (Pattern 4: Auto-Save with Debounce)
  </read_first>
  <action>
    1. **src/components/settings/settings-section.tsx**: Reusable section wrapper (per D-04):
       - Props: `{ title: string, description?: string, children: React.ReactNode }`
       - Renders a `<section>` with dark card styling: `bg-neutral-900 rounded-lg border border-neutral-800 p-6`
       - Title as `<h2>` in `text-lg font-semibold text-neutral-100`
       - Description as `<p>` in `text-sm text-neutral-400 mt-1`
       - Children rendered below with `mt-4 space-y-4`
       - `'use client'` not needed (server component wrapper)

    2. **src/components/settings/mqtt-settings.tsx**: MQTT broker config (per D-04, D-05, D-06):
       - `'use client'` directive
       - Props: `{ initialSettings: Record<string, string> }`
       - State for four fields: host (`mqtt.host`), port (`mqtt.port`), username (`mqtt.username`), password (`mqtt.password`)
       - Initialize from `initialSettings` prop
       - Each input field: dark styled (`bg-neutral-800 border-neutral-700 text-neutral-100 rounded-md px-3 py-2 w-full focus:ring-2 focus:ring-blue-500 focus:border-transparent`)
       - Labels in `text-sm font-medium text-neutral-300`
       - Auto-save: implement `useAutoSave` hook per RESEARCH.md Pattern 4. On each field change, debounce 500ms, then `fetch('/api/settings', { method: 'PUT', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ key, value }) })`. Show subtle save indicator: small text "Gespeichert" in `text-green-400` that fades after 2 seconds.
       - Test button: styled button (`bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-md`). On click, POST to `/api/mqtt/test` with current host/port/username/password values. While testing, show spinner text "Teste Verbindung...". On success: show "Verbindung erfolgreich" in green. On failure: show "Verbindung fehlgeschlagen" in red. Result displayed inline below the button.
       - Layout: 2-column grid for host/port (host takes more space), single column for username/password, test button below.

    3. **src/components/settings/pushover-settings.tsx**: Pushover credentials (per SETT-02):
       - `'use client'` directive
       - Props: `{ initialSettings: Record<string, string> }`
       - State for two fields: userKey (`pushover.userKey`), apiToken (`pushover.apiToken`)
       - Same auto-save pattern as MQTT settings
       - Same input styling as MQTT settings
       - Two single-column input fields with labels "User Key" and "API Token"
       - Password-type input for API token (can toggle visibility)

    4. **src/app/settings/page.tsx**: Settings page (per D-04):
       - Default export async function `SettingsPage`
       - Server component: fetch all settings from DB directly (import db and config table, select all, convert to Record<string, string>)
       - Render page title "Einstellungen" in `text-2xl font-bold text-neutral-100`
       - Render two SettingsSection wrappers:
         a. Title "MQTT Broker", description "Verbindungseinstellungen fuer den MQTT-Broker" -- contains `<MqttSettings initialSettings={settings} />`
         b. Title "Pushover", description "Benachrichtigungen via Pushover" -- contains `<PushoverSettings initialSettings={settings} />`
       - Layout: `max-w-2xl mx-auto py-8 px-4 space-y-6`
  </action>
  <verify>
    <automated>cd /Users/hulki/codex/charging-master && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/settings/page.tsx contains `export default async function SettingsPage` and `MqttSettings` and `PushoverSettings`
    - src/app/settings/page.tsx contains `db.select()` (server-side data loading)
    - src/components/settings/mqtt-settings.tsx contains `'use client'` and `fetch('/api/settings'` and `fetch('/api/mqtt/test'`
    - src/components/settings/mqtt-settings.tsx contains `mqtt.host` and `mqtt.port` and `useAutoSave` or debounce logic with `setTimeout`
    - src/components/settings/pushover-settings.tsx contains `'use client'` and `pushover.userKey` and `pushover.apiToken`
    - src/components/settings/settings-section.tsx contains `bg-neutral-900` and `border-neutral-800`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Settings page at /settings shows MQTT and Pushover sections, all inputs auto-save on change with debounce, MQTT test button shows inline connection result, dark theme consistent with D-03</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- zero type errors
2. Navigate to http://localhost:3000/settings -- page loads with MQTT and Pushover sections
3. Enter MQTT host "mqtt-master.local", wait 500ms -- value persists (check via GET /api/settings)
4. Click "Verbindung testen" -- shows success/failure inline
5. Refresh page -- settings values are preserved from database
</verification>

<success_criteria>
- /settings page renders with MQTT Broker and Pushover sections
- Changing any input auto-saves after 500ms debounce
- MQTT test button sends POST to /api/mqtt/test and shows inline result
- All settings persist in SQLite config table and survive page refresh
- Dark theme styling consistent with D-03 (dark surfaces, neutral colors, subtle borders)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
