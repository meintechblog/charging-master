---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/page.tsx
  - src/app/devices/page.tsx
  - src/app/api/devices/route.ts
  - src/app/api/devices/discover/route.ts
  - src/components/layout/sidebar.tsx
  - src/components/layout/app-shell.tsx
  - src/components/devices/plug-card.tsx
  - src/components/devices/discovery-list.tsx
  - src/components/devices/add-device-form.tsx
  - src/app/layout.tsx
autonomous: true
requirements:
  - SHLY-01
  - SHLY-06
must_haves:
  truths:
    - "User can see all registered Shelly Plugs as cards on the dashboard with online/offline status and current power"
    - "User can add a Shelly Plug via MQTT auto-discovery (select from discovered list)"
    - "User can add a Shelly Plug manually by entering topic prefix as fallback"
    - "App shell has a sidebar with navigation links to Dashboard, Devices, Settings"
    - "Multiple plugs are displayed simultaneously"
  artifacts:
    - path: "src/components/layout/sidebar.tsx"
      provides: "Navigation sidebar with links"
      contains: "Dashboard"
    - path: "src/components/devices/plug-card.tsx"
      provides: "Individual plug status card"
      contains: "apower"
    - path: "src/app/devices/page.tsx"
      provides: "Device management with discovery and manual add"
      contains: "DiscoveryList"
    - path: "src/app/api/devices/route.ts"
      provides: "Device CRUD API"
      exports: ["GET", "POST", "DELETE"]
  key_links:
    - from: "src/app/page.tsx"
      to: "/api/devices"
      via: "Server-side DB query for plug list"
      pattern: "db\\.select.*from\\(plugs\\)"
    - from: "src/app/api/devices/discover/route.ts"
      to: "globalThis.__discoveredDevices"
      via: "Reads pre-populated discovered devices Map from server.ts (Plan 01-01)"
      pattern: "globalThis.__discoveredDevices"
    - from: "src/components/devices/plug-card.tsx"
      to: "src/db/schema.ts"
      via: "Plug type from schema inference"
      pattern: "plugs"
---

<objective>
Build the app shell (sidebar navigation per D-01), dashboard with plug cards (per D-02), and device management page with MQTT auto-discovery (per D-07) and manual add fallback (per D-08).

Purpose: Users need to see their Shelly Plugs at a glance (SHLY-06) and add new ones (SHLY-01). The sidebar establishes the app's navigation structure for all future phases.
Output: Working dashboard with plug cards, device management page with discovery, and app-wide sidebar navigation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

Depends on Plan 01 for: src/db/client.ts, src/db/schema.ts, src/modules/mqtt/mqtt-service.ts, src/modules/mqtt/discovery.ts, src/modules/events/event-bus.ts, src/types/global.d.ts, src/app/layout.tsx, server.ts (which populates globalThis.__discoveredDevices).

<interfaces>
From src/db/schema.ts (created in Plan 01):
```typescript
export const plugs = sqliteTable('plugs', {
  id: text('id').primaryKey(),              // shellyplugsg3-AABBCC
  name: text('name').notNull(),
  mqttTopicPrefix: text('mqtt_topic_prefix').notNull(),
  ipAddress: text('ip_address'),
  pollingInterval: integer('polling_interval').notNull().default(5),
  enabled: integer('enabled', { mode: 'boolean' }).notNull().default(true),
  online: integer('online', { mode: 'boolean' }).notNull().default(false),
  lastSeen: integer('last_seen'),
  createdAt: integer('created_at').notNull(),
  updatedAt: integer('updated_at').notNull(),
});
```

From src/modules/mqtt/discovery.ts (created in Plan 01):
```typescript
export const DISCOVERY_TOPICS: string[];
export function parseDeviceId(topic: string): string;
export function isDiscoveryTopic(topic: string): boolean;
export interface DiscoveredDevice { deviceId: string; firstSeen: number; lastSeen: number; online: boolean; }
```

From src/modules/mqtt/mqtt-service.ts (created in Plan 01):
```typescript
export class MqttService {
  subscribeToPlug(plugId: string): void;
  unsubscribeFromPlug(plugId: string): void;
  isConnected(): boolean;
}
```

globalThis.__mqttService: MqttService
globalThis.__eventBus: EventBus
globalThis.__discoveredDevices: Map<string, DiscoveredDevice> (populated by server.ts online:* listener in Plan 01-01)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: App shell (sidebar + layout wrapper per D-01, D-03) and device API routes</name>
  <files>
    src/components/layout/sidebar.tsx,
    src/components/layout/app-shell.tsx,
    src/app/layout.tsx,
    src/app/api/devices/route.ts,
    src/app/api/devices/discover/route.ts
  </files>
  <read_first>
    src/app/layout.tsx (current root layout from Plan 01)
    src/db/schema.ts (plugs table definition)
    src/db/client.ts (db export)
    src/modules/mqtt/mqtt-service.ts (subscribeToPlug, isConnected)
    src/modules/mqtt/discovery.ts (DiscoveredDevice type, parseDeviceId)
    src/types/global.d.ts (globalThis types including __discoveredDevices)
    .planning/phases/01-foundation/01-CONTEXT.md (D-01 sidebar layout, D-03 dark theme, D-07 discovery, D-08 manual add, D-09 per-plug config)
  </read_first>
  <action>
    1. **src/components/layout/sidebar.tsx**: Navigation sidebar (per D-01):
       - `'use client'` directive (needs usePathname for active link highlighting)
       - Import `usePathname` from `next/navigation`
       - Render `<nav>` with `w-64 h-screen bg-neutral-900 border-r border-neutral-800 flex flex-col p-4`
       - App title at top: "Charging Master" in `text-lg font-bold text-neutral-100 mb-8`
       - Navigation links as `<a>` tags (use Next.js `Link` from `next/link`):
         a. Dashboard: href="/", icon placeholder (can use unicode or SVG later)
         b. Geraete: href="/devices"
         c. Einstellungen: href="/settings"
         d. (Verlauf: href="/history" -- greyed out, Phase 4 placeholder)
       - Each link: `flex items-center gap-3 px-3 py-2 rounded-md text-sm font-medium`. Active: `bg-neutral-800 text-neutral-100`. Inactive: `text-neutral-400 hover:text-neutral-100 hover:bg-neutral-800/50`.
       - Active detection: `pathname === href` or `pathname.startsWith(href)` for non-root paths.
       - Bottom of sidebar: MQTT connection status indicator. Small dot (green if connected, red if not) with text "MQTT" and status. This is a visual-only element -- actual status comes from a prop or could be static for now.

    2. **src/components/layout/app-shell.tsx**: Sidebar + content wrapper (per D-01):
       - Server component (no `'use client'`)
       - Props: `{ children: React.ReactNode }`
       - Renders: `<div className="flex h-screen overflow-hidden">` containing `<Sidebar />` and `<main className="flex-1 overflow-y-auto bg-neutral-950 p-6">{children}</main>`

    3. **src/app/layout.tsx**: Update root layout (created in Plan 01) to wrap children with AppShell:
       - Keep existing `<html lang="de" class="dark">` and `<body>` with dark background
       - Import `AppShell` from `@/components/layout/app-shell`
       - Wrap `{children}` with `<AppShell>{children}</AppShell>`

    4. **src/app/api/devices/route.ts**: Device CRUD API:
       - `GET`: Select all plugs from DB, return as JSON array via `Response.json(allPlugs)`.
       - `POST`: Accept JSON body `{ id: string, name: string, mqttTopicPrefix: string, ipAddress?: string, pollingInterval?: number }`. Validate id and name are non-empty strings. Insert into plugs table with `enabled: true`, `online: false`, `createdAt: Date.now()`, `updatedAt: Date.now()`. After insert, call `globalThis.__mqttService.subscribeToPlug(id)`. Return `Response.json(newPlug, { status: 201 })`. On duplicate id, return `Response.json({ error: "device_exists" }, { status: 409 })`.
       - `DELETE`: Accept JSON body `{ id: string }`. Delete from plugs table where id matches. Call `globalThis.__mqttService.unsubscribeFromPlug(id)`. Return `Response.json({ ok: true })`. If not found, return `Response.json({ error: "not_found" }, { status: 404 })`.

    5. **src/app/api/devices/discover/route.ts**: MQTT discovery endpoint (per D-07):
       - `export const runtime = 'nodejs'`
       - `GET`: Check if MQTT is connected via `globalThis.__mqttService.isConnected()`. If not, return `Response.json({ error: "mqtt_not_connected", devices: [] }, { status: 503 })`. If connected, read discovered devices directly from `globalThis.__discoveredDevices` (this Map is already populated by the `online:*` listener set up in server.ts by Plan 01-01). Return `Response.json({ devices: Array.from(globalThis.__discoveredDevices.values()) })`. If the map is undefined (edge case), return empty array.
  </action>
  <verify>
    <automated>cd /Users/hulki/codex/charging-master && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/layout/sidebar.tsx contains `'use client'`, `usePathname`, `Dashboard`, `Geraete`, `Einstellungen`, `bg-neutral-900`
    - src/components/layout/app-shell.tsx contains `Sidebar` and `flex h-screen`
    - src/app/layout.tsx contains `AppShell`
    - src/app/api/devices/route.ts contains `export async function GET`, `export async function POST`, `export async function DELETE`, `subscribeToPlug`
    - src/app/api/devices/discover/route.ts contains `export async function GET` and `isConnected` and `__discoveredDevices`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>App has sidebar navigation with Dashboard/Geraete/Einstellungen links, dark theme styling, device CRUD API, and MQTT discovery endpoint reading from pre-populated globalThis.__discoveredDevices</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard plug cards and device management page (per D-02, D-07, D-08, D-09)</name>
  <files>
    src/app/page.tsx,
    src/app/devices/page.tsx,
    src/components/devices/plug-card.tsx,
    src/components/devices/discovery-list.tsx,
    src/components/devices/add-device-form.tsx
  </files>
  <read_first>
    src/db/schema.ts (plugs table for type inference)
    src/db/client.ts (db export for server-side queries)
    src/app/api/devices/route.ts (API contract from Task 1)
    src/app/api/devices/discover/route.ts (discovery API from Task 1)
    src/components/layout/sidebar.tsx (styling patterns from Task 1)
    .planning/phases/01-foundation/01-CONTEXT.md (D-02 plug cards, D-07 auto-discovery, D-08 manual add, D-09 per-plug config)
  </read_first>
  <action>
    1. **src/components/devices/plug-card.tsx**: Individual plug status card (per D-02):
       - `'use client'` directive (will need real-time updates in Phase 2, prepare now)
       - Props: `{ plug: { id: string, name: string, online: boolean, enabled: boolean, lastSeen: number | null } }`
       - Card styling: `bg-neutral-900 rounded-lg border border-neutral-800 p-4 hover:border-neutral-700 transition-colors`
       - Top row: plug name in `text-base font-medium text-neutral-100`, online indicator dot (green `bg-green-500` if online, red `bg-red-500` if offline) with text "Online"/"Offline" in `text-xs text-neutral-400`
       - Middle row: power value placeholder showing "--" W (real-time power will come from SSE in Phase 2). Style: `text-2xl font-bold text-neutral-100` for the number, `text-sm text-neutral-400` for "W"
       - Bottom row: last seen timestamp (if exists) formatted as relative time (e.g., "vor 2 Minuten"). Use simple relative time calculation (no library needed). If null, show "Noch nie gesehen" in `text-neutral-500`.
       - If `enabled === false`, overlay entire card with `opacity-50` styling

    2. **src/app/page.tsx**: Dashboard page (per D-02):
       - Default export async function `HomePage` (server component)
       - Query all plugs from DB: `const allPlugs = db.select().from(plugs).all()`
       - Page title: "Dashboard" in `text-2xl font-bold text-neutral-100 mb-6`
       - If no plugs: show empty state card with message "Keine Geraete hinzugefuegt" and a link to "/devices" ("Geraet hinzufuegen" as blue link)
       - If plugs exist: render as responsive grid `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4`
       - Each plug rendered as `<PlugCard plug={plug} />`

    3. **src/components/devices/discovery-list.tsx**: MQTT auto-discovery results (per D-07):
       - `'use client'` directive
       - State: `discoveredDevices` array, `loading` boolean, `error` string | null
       - On mount (useEffect): fetch `GET /api/devices/discover`. Populate state. Poll every 5 seconds while component is mounted (clear interval on unmount).
       - Render list of discovered devices as selectable rows. Each row shows device ID (e.g., `shellyplugsg3-AABBCC`) and online/offline status.
       - Each row has an "Hinzufuegen" button that calls a callback prop `onAddDevice(deviceId: string)`.
       - Already-registered devices (compare against `registeredIds` prop) shown as greyed-out "Bereits hinzugefuegt".
       - If MQTT not connected: show info message "MQTT nicht verbunden. Verbindung in Einstellungen konfigurieren." with link to /settings.
       - If no devices found: show "Suche nach Shelly-Geraeten..." with subtle animation (pulse dot).
       - Styling: dark list items `bg-neutral-800 rounded-md p-3 flex items-center justify-between`

    4. **src/components/devices/add-device-form.tsx**: Manual device entry fallback (per D-08):
       - `'use client'` directive
       - Form with fields: Device ID (text, placeholder "shellyplugsg3-AABBCC"), Name/Alias (text, placeholder "Wohnzimmer Ladegeraet"), IP-Adresse (text, optional, placeholder "192.168.3.167")
       - Submit button "Geraet hinzufuegen" (`bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-md`)
       - On submit: POST to `/api/devices` with `{ id, name, mqttTopicPrefix: id, ipAddress }`. On 201 success: call `onAdded()` callback prop and clear form. On 409: show inline error "Geraet bereits registriert" in red. On other errors: show generic error.
       - Dark input styling consistent with settings page: `bg-neutral-800 border-neutral-700 text-neutral-100 rounded-md px-3 py-2`

    5. **src/app/devices/page.tsx**: Device management page (per D-07, D-08, D-09):
       - Default export async function `DevicesPage` (server component)
       - Query registered plug IDs from DB for passing to discovery list
       - Page title: "Geraete" in `text-2xl font-bold text-neutral-100 mb-6`
       - Two sections separated by layout:
         a. "Geraete-Erkennung" (auto-discovery per D-07): `<DiscoveryList registeredIds={registeredIds} onAddDevice={...} />` wrapped in a section card (`bg-neutral-900 rounded-lg border border-neutral-800 p-6`). The onAddDevice handler should trigger a form submission to the devices API.
         b. "Manuell hinzufuegen" (manual fallback per D-08): collapsible section (details/summary or toggle), contains `<AddDeviceForm onAdded={...} />`
       - Both wrapped in a client component wrapper that handles the add flow and router.refresh() after adding.
       - Below: "Registrierte Geraete" section showing existing plugs as a simple list with name, ID, enabled status, and a delete button (calls DELETE /api/devices).
  </action>
  <verify>
    <automated>cd /Users/hulki/codex/charging-master && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/page.tsx contains `export default async function HomePage` and `db.select()` and `PlugCard` and `grid grid-cols`
    - src/components/devices/plug-card.tsx contains `'use client'`, `bg-neutral-900`, `bg-green-500`, `bg-red-500`, `apower` or "W"
    - src/components/devices/discovery-list.tsx contains `'use client'`, `api/devices/discover`, `onAddDevice`, `shellyplugsg3`
    - src/components/devices/add-device-form.tsx contains `'use client'`, `api/devices`, `POST`, `mqttTopicPrefix`
    - src/app/devices/page.tsx contains `export default`, `DiscoveryList` or `discovery`, `AddDeviceForm` or `add-device`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Dashboard shows plug cards with online/offline status in a responsive grid, Devices page offers MQTT auto-discovery and manual add form, users can add/remove Shelly Plugs, dark theme consistent throughout</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- zero type errors
2. Navigate to http://localhost:3000 -- dashboard shows "Keine Geraete" or plug cards if any exist
3. Navigate to http://localhost:3000/devices -- discovery list attempts to find Shelly devices, manual add form available
4. Sidebar visible on all pages with active link highlighting
5. Add a device manually, verify it appears on dashboard as a card
6. Delete a device, verify it disappears
</verification>

<success_criteria>
- App shell renders sidebar with navigation links (Dashboard, Geraete, Einstellungen) on all pages
- Dashboard shows registered Shelly Plugs as cards with online/offline status and power placeholder
- Device management page has MQTT auto-discovery list and manual add fallback form
- Users can add and remove Shelly Plugs via the UI
- Multiple plugs displayed simultaneously in responsive grid
- All UI follows dark theme (D-03) with neutral-900/800/700 color scheme
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
