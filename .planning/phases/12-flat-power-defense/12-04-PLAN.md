---
phase: 12-flat-power-defense
plan: 04
type: execute
wave: 3
depends_on: [12-01, 12-02, 12-03]
files_modified:
  - src/components/charging/charge-banner.tsx
  - src/components/charging/charge-banner.test.tsx
  - src/components/settings/charging-settings.tsx
autonomous: true
requirements: [FPD-05]
must_haves:
  truths:
    - "ChargeBanner shows a yellow 'Watchdog: 0 W seit Xs' indicator when watchdogKind === 'warning' from the SSE charge event, with a CSS-animated countdown bar (width = secondsAtZero / stalePowerWindowSec)."
    - "ChargeBanner shows a red 'Session abgebrochen — Battery full?' banner when watchdogKind === 'fired', with an Acknowledge button."
    - "Acknowledge button persists dismissal in localStorage keyed by sessionId (key: `charging-watchdog-ack-${sessionId}`); banner stays hidden after reload until a new session starts."
    - "ChargingSettings page exposes 5 new Advanced inputs: stalePowerThresholdW (W), stalePowerWindowSec (s), matcherRefreshReadings (n), lowConfidenceThreshold (0..1), maxSessionHours (h). Each uses useAutoSave pattern with 500ms debounce."
  artifacts:
    - path: src/components/charging/charge-banner.tsx
      provides: "WatchdogWarning + WatchdogFired sub-renders + acknowledge button + localStorage dismissal"
      contains: "watchdogKind"
    - path: src/components/charging/charge-banner.test.tsx
      provides: "RTL tests for none/warning/fired states + Acknowledge flow"
      contains: "watchdog"
    - path: src/components/settings/charging-settings.tsx
      provides: "5 new advanced inputs for FPD config rows"
      contains: "stalePowerThresholdW"
success_gates:
  - command: pnpm exec tsc --noEmit
    expected: exit 0
  - command: pnpm exec vitest run src/components/charging/charge-banner.test.tsx
    expected: all pass
  - command: grep -n "watchdogKind\\|watchdog-ack" src/components/charging/charge-banner.tsx | wc -l
    expected: >= 3
  - command: grep -n "stalePowerThresholdW\\|matcherRefreshReadings\\|lowConfidenceThreshold\\|maxSessionHours\\|stalePowerWindowSec" src/components/settings/charging-settings.tsx | wc -l
    expected: >= 5
---

<objective>
Ship FPD-05 (UI Watchdog Indicators) plus the Settings-page exposure of all five new Phase-12 config rows. This is the user-visible surface of the entire phase — without 12-04, the watchdogs work silently. Backend events from 12-01/12-02/12-03 are already flowing through the SSE `/api/sse/power` stream and ChargeStateEvent fields (`watchdogKind`, `stalePowerSeconds`, `stalePowerFiresAt`, `stopMode`); this plan consumes them.

**Banner sub-states** (yellow warning + red fired) extend the existing `ChargeBanner` component instead of creating a new `watchdog-banner.tsx` — keeps the banner state machine in one place and avoids two banners stacking. The Acknowledge button uses localStorage by `sessionId` (resolved Open Question OQ-4 + PATTERNS Pattern 12) so dismissal is naturally session-scoped: a new session has a new sessionId, key not present, banner re-arms.

**Settings page** mirrors the existing Advanced section pattern verbatim. Each new input uses the duplicated `useAutoSave` hook (acknowledged backlog item in current code — do NOT refactor per PATTERNS Pattern 8 note).

Wave 3. Depends on all three backend plans for the SSE event fields to exist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12-flat-power-defense/CONTEXT.md
@.planning/phases/12-flat-power-defense/RESEARCH.md
@.planning/phases/12-flat-power-defense/PATTERNS.md
@.planning/phases/12-flat-power-defense/12-01-SUMMARY.md
@.planning/phases/12-flat-power-defense/12-02-SUMMARY.md
@.planning/phases/12-flat-power-defense/12-03-SUMMARY.md
@src/components/charging/charge-banner.tsx
@src/components/charging/soc-band-indicator.tsx
@src/components/charging/soc-band-indicator.test.tsx
@src/components/settings/charging-settings.tsx
@src/modules/charging/types.ts
</context>

<interfaces>
<!-- Event contract from backend plans -->

From src/modules/charging/types.ts (after 12-01..03 land):
```typescript
export interface ChargeStateEvent {
  plugId: string;
  state: ChargeState;
  sessionId?: number;
  socMin?: number; socMax?: number; socBandConfidence?: number; socAsciiBar?: string;
  // Phase 12 additions:
  watchdogKind?: 'none' | 'warning' | 'fired';
  stalePowerSeconds?: number;     // current count * 5s
  stalePowerFiresAt?: number;     // epoch ms, undefined when not 'warning'
  stopMode?: 'aggressive' | 'conservative' | 'energy_fallback';
}
```

From src/components/charging/soc-band-indicator.tsx (CSS-variable pattern to mirror):
```tsx
type CSSVarStyle = CSSProperties & Record<`--${string}`, string>;
// container style={{ '--soc-min': `${band.min}%` } as CSSVarStyle}
// inner div style={{ width: 'calc(var(--soc-max) - var(--soc-min))' }}
// classes: "transition-all duration-700 ease-out"
```

From src/components/charging/soc-band-indicator.test.tsx (RTL pattern):
```tsx
vi.mock('@/hooks/use-charge-stream', () => ({ useChargeStream: vi.fn() }));
let cb: ((e: ChargeStateEvent) => void) | null = null;
(useChargeStream as Mock).mockImplementation((_, fn) => { cb = fn; });
function emit(e: Partial<ChargeStateEvent>) {
  act(() => cb?.({ plugId: 'plug-1', state: 'charging', ...e } as ChargeStateEvent));
}
```

From src/components/settings/charging-settings.tsx (useAutoSave + advanced input pattern, lines 13-37, 121-148):
```tsx
function useAutoSave(key: string, value: string, initial: string) {
  // 500ms debounce, POSTs to /api/config or similar — read existing impl
}
// Advanced section: number input + label + save-status indicator. Repeat per key.
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ChargeBanner watchdog warning + fired sub-renders with localStorage ack</name>
  <files>
    src/components/charging/charge-banner.tsx,
    src/components/charging/charge-banner.test.tsx
  </files>
  <behavior>
    - Read the existing ChargeBanner (src/components/charging/charge-banner.tsx, 553 lines). Find the main render switch on `state`/`flow` and identify a stable insertion point for the watchdog sub-render. The watchdog warning should render INSIDE the 'charging' state branch (next to the existing detection-progress bar at lines ~290-305 per PATTERNS). The 'fired' banner replaces the entire state block when watchdogKind === 'fired' (the underlying SSE state will be 'aborted' anyway).
    - Track `watchdogKind`, `stalePowerSeconds`, `stalePowerFiresAt` in the component via the existing `useChargeStream(plugId, cb)` subscription pattern (line ~35). Store the last 'fired' event in component state so the banner persists after the SSE stream emits subsequent `'none'`-kind events post-abort.
    - Yellow warning sub-render:
      - Visible when `watchdogKind === 'warning'` AND state ∈ {charging, countdown}.
      - Reads `secondsAtZero = stalePowerSeconds ?? 0`. `windowSec` is NOT in the event — derive `firesIn = Math.max(0, (stalePowerFiresAt ?? 0) - Date.now()) / 1000` and the bar fraction is `1 - firesIn / (firesIn + secondsAtZero)` (= secondsAtZero / windowSec). Acceptable approximation; document inline.
      - Markup mirrors detection-progress-bar (lines 290-305 of charge-banner.tsx): outer `relative h-1.5 bg-neutral-800 rounded-full overflow-hidden`, inner `h-full bg-amber-400 transition-[width] duration-500` with `style={{ width: \`${fraction * 100}%\` }}`.
      - Label: `Watchdog: 0 W seit ${Math.round(secondsAtZero)}s` next to bar.
      - Data-testid `watchdog-warning`.
    - Red fired sub-render:
      - Visible when last received 'fired' event for the current sessionId is in local state AND localStorage `charging-watchdog-ack-${sessionId}` is NOT set.
      - Markup: red bg (`bg-red-900/40 border-red-700`), text 'Session abgebrochen — Battery full?', Acknowledge button (`<button data-testid="watchdog-ack">Verstanden</button>`).
      - On click: `localStorage.setItem(\`charging-watchdog-ack-\${sessionId}\`, '1')` + setState to hide.
      - Data-testid `watchdog-fired`.
    - Local state initialization for the ack flag:
      ```tsx
      const [acked, setAcked] = useState(() => {
        if (typeof window === 'undefined' || sessionId === undefined) return false;
        return localStorage.getItem(`charging-watchdog-ack-${sessionId}`) === '1';
      });
      ```
      Reset to false when sessionId changes (new session).
    - Tests in charge-banner.test.tsx (NEW file unless one already exists — check first; if exists, EXTEND):
      - `useChargeStream` mock pattern from soc-band-indicator.test.tsx.
      - **none kind:** emit `{watchdogKind: 'none'}` → no watchdog markup (`queryByTestId('watchdog-warning')` is null).
      - **warning kind:** emit `{watchdogKind: 'warning', stalePowerSeconds: 60, stalePowerFiresAt: Date.now() + 240_000}` → `getByTestId('watchdog-warning')` exists, label contains '60s', bar width ≈ 20%.
      - **fired kind:** emit `{state: 'aborted', watchdogKind: 'fired', sessionId: 7}` → `getByTestId('watchdog-fired')` exists.
      - **ack flow:** emit 'fired' for sessionId=7. Click `getByTestId('watchdog-ack')`. Assert `getByTestId('watchdog-fired')` no longer in document. Assert `localStorage.getItem('charging-watchdog-ack-7') === '1'`.
      - **ack persistence:** preset localStorage 'charging-watchdog-ack-7'='1' before render. Emit 'fired' for sessionId=7. Assert banner not visible (initial state honored localStorage).
      - **new session re-arms:** preset 'charging-watchdog-ack-7'='1'. Emit 'fired' for sessionId=8 (different). Banner SHOULD be visible (different key not in storage).
  </behavior>
  <action>
    Read charge-banner.tsx start-to-finish ONCE; locate the render switch and identify which branch handles 'charging' and 'aborted'. Add the watchdog state extraction at the top of the component (after existing useChargeStream subscription). Wire the warning sub-render inside the 'charging'/'countdown' branch. Wire the fired sub-render inside the 'aborted' branch (or as a top-level overlay if 'aborted' branch is already producing a different banner — judgment call; document in SUMMARY). For tests, if `charge-banner.test.tsx` does not exist, CREATE it following soc-band-indicator.test.tsx structure (vi.mock + cb pattern). Do NOT use vi.useFakeTimers — emit-driven re-renders per RESEARCH §FPD-05 Q3 recommendation. Each test wraps emits in `act(...)`. Clear localStorage between tests via `beforeEach(() => localStorage.clear())`.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/components/charging/charge-banner.test.tsx</automated>
  </verify>
  <done>
    All six watchdog-banner tests pass. tsc clean. `grep "watchdogKind" src/components/charging/charge-banner.tsx | wc -l` >= 3. Existing charge-banner tests (if any) still green.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ChargingSettings — 5 new Advanced inputs for FPD config rows</name>
  <files>
    src/components/settings/charging-settings.tsx
  </files>
  <behavior>
    - Read existing ChargingSettings (152 lines) — locate the Advanced section at lines ~121-148 and the bandThreshold input pattern at lines ~58-62.
    - Add 5 new inputs to the Advanced section, in this display order:
      1. **Stale-Power Schwellwert (W)** — key `charging.stalePowerThresholdW`, default `"1.0"`, parse float, validate `parsed > 0 && parsed < 50` (sane plug-side power range), step 0.1.
      2. **Stale-Power Fenster (s)** — key `charging.stalePowerWindowSec`, default `"300"`, parse integer, validate `parsed > 0 && parsed <= 3600`, step 60.
      3. **Matcher-Refresh (Readings)** — key `charging.matcherRefreshReadings`, default `"60"`, parse integer, validate `parsed > 0 && parsed <= 3600`, step 10.
      4. **Confidence-Schwellwert (0..1)** — key `charging.lowConfidenceThreshold`, default `"0.5"`, parse float, validate `parsed > 0 && parsed <= 1`, step 0.05.
      5. **Max Session-Dauer (Stunden)** — key `charging.maxSessionHours`, default `"24"`, parse integer, validate `parsed > 0 && parsed <= 168` (1 week ceiling), step 1.
    - Each input replicates the existing bandThreshold pattern exactly: `useAutoSave(key, value, initial)`, validation gate that uses validated string for save (lines 53-57 model), save-status indicator.
    - Add a section heading or visual divider before the new inputs labeled "Flat-Power Defense (Phase 12)" so users can find them.
    - Hover/title text on each input briefly explaining purpose (German, matching codebase voice). E.g., on stalePowerThresholdW: "Unterhalb dieser Wattzahl gilt der Plug als 'stale' (Standard: 1.0 W)".
  </behavior>
  <action>
    Read the existing charging-settings.tsx ONCE. Identify the constants used for input styling (`inputClasses`, `labelClasses`). For each new input, copy the bandThreshold block verbatim, change the config key, default, parse function, and validation predicate. Tests for the Settings page are NOT in scope — this is a follow-the-pattern UI change and the underlying `useAutoSave` already has its own test coverage. Visual smoke is covered by manual on-device test in CONTEXT DoD. The five inputs render under a new `<h3>Flat-Power Defense</h3>` subsection inside Advanced.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && grep -c "useAutoSave('charging\\.stalePowerThresholdW\\|useAutoSave('charging\\.stalePowerWindowSec\\|useAutoSave('charging\\.matcherRefreshReadings\\|useAutoSave('charging\\.lowConfidenceThreshold\\|useAutoSave('charging\\.maxSessionHours" src/components/settings/charging-settings.tsx</automated>
  </verify>
  <done>
    Type-check clean. Grep confirms five new useAutoSave call sites. Visual section "Flat-Power Defense" appears in Advanced.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify watchdog UI on dev server</name>
  <what-built>
    ChargeBanner shows yellow warning + red fired states from SSE events. Acknowledge button persists dismissal per sessionId. Settings → Charging → Advanced → "Flat-Power Defense" section exposes all five Phase-12 config inputs with auto-save.
  </what-built>
  <how-to-verify>
    1. `pnpm dev` then open http://localhost:3000.
    2. Visit /settings — scroll to "Charging" → "Advanced" → "Flat-Power Defense". Verify all 5 inputs render: Stale-Power Schwellwert (W), Stale-Power Fenster (s), Matcher-Refresh (Readings), Confidence-Schwellwert, Max Session-Dauer (Stunden). Edit one (e.g., set Stale-Power Fenster to 120) and confirm "Saved" indicator appears.
    3. Open the dashboard. If no active session exists, optionally use the dev-mode /api/sse/power feed to push synthetic events OR plug in a device + wait for a 'charging' state.
    4. Synthetic verify path: load the dashboard in two browser tabs. Open devtools → Network → SSE event `charge`. Confirm the event payload includes `watchdogKind`, `stalePowerSeconds`, `stopMode` (one of the values, possibly undefined when none).
    5. Verify the yellow warning is not present in a normal high-power session.
    6. If you have an active session, induce a 'warning' state by turning the relay off manually (POST `/api/devices/<id>/relay {command:'off'}`) and waiting ≥60s. The yellow watchdog bar should appear in the ChargeBanner.
    7. Continue waiting 5 minutes. The session should abort with the red "Session abgebrochen — Battery full?" banner + Acknowledge button. Click Acknowledge — banner disappears.
    8. Reload the page — banner stays gone (localStorage persisted).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE → browser | Event payload is trusted (server-emitted from internal Maps); no XSS risk. |
| localStorage | Single-user app on LAN; ack-state is local convenience, not security-relevant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-10 | Tampering | localStorage ack key collision | accept | Key includes sessionId; collision requires sessionId reuse which the DB autoincrement prevents. |
| T-12-11 | Information disclosure | watchdog banner text | accept | Profile name in the banner is already exposed elsewhere on the dashboard. |
</threat_model>

<verification>
- `pnpm exec tsc --noEmit` exits 0.
- `pnpm exec vitest run src/components/charging/charge-banner.test.tsx` all pass.
- `grep -n "watchdogKind\\|charging-watchdog-ack" src/components/charging/charge-banner.tsx | wc -l` >= 3.
- `grep -n "charging\\.stalePowerThresholdW\\|charging\\.stalePowerWindowSec\\|charging\\.matcherRefreshReadings\\|charging\\.lowConfidenceThreshold\\|charging\\.maxSessionHours" src/components/settings/charging-settings.tsx | wc -l` >= 5.
</verification>

<success_criteria>
FPD-05 acceptance:
- Yellow "Watchdog: 0 W seit Xs" indicator renders when `apower < threshold` for > 60s (= 20% of default 300s window).
- Red "Session abgebrochen — Battery full?" banner renders when FPD-01 fires.
- Acknowledge button hides banner + persists dismissal via localStorage keyed by sessionId.
- Reload preserves dismissal.
- New session re-arms banner (different sessionId → different key).

Settings exposure:
- All 5 Phase-12 config rows accessible in /settings → Charging → Advanced → "Flat-Power Defense" section.
- Inputs use existing useAutoSave pattern (500ms debounce) with validation + save-status indicator.

End-to-end phase complete:
- FPD-01..05 all acceptance criteria met across plans 12-01..04.
- 171 baseline tests + new tests for stale-power, adaptive matcher, energy fallback, timeout, and watchdog UI all green.
- tsc clean.
- Human verify passes on dev server.
</success_criteria>

<output>
After completion, create `.planning/phases/12-flat-power-defense/12-04-SUMMARY.md` summarizing:
- ChargeBanner extension approach (sub-render vs separate component decision)
- localStorage ack key shape + lifecycle (set on click, read on init, ignored on new sessionId)
- Five new Settings inputs + their validation ranges
- RTL test coverage (6 scenarios)
- Human-verify outcome
</output>
