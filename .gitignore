node_modules/
.next/
data/
.data.test/
.env
.env.local

# Generated at build time — environment-specific
/src/lib/version.ts

# Runtime update state — local to each machine
/.update-state/

# Local working state — must not enter the catalog repo
/.claude/
/.planning/
/.tmp-research/

# TypeScript incremental build cache (perpetually-dirty artifact)
*.tsbuildinfo
